# Session Sync

Date: 2026-07-03

This is the shared handoff file for the clean v2 Nornickel hackathon workspace.

Canonical active workdir: `/Volumes/T7_2TB/Projects-T7_2TB/2026_Nornikel_Hackaton_v2`. Start new local work in this v2 checkout, not in the older `../2026_Nornikel_Hackaton` repository unless explicitly requested.

GitHub repository: `https://github.com/s1mb1o/2026-ore-thin-section-classifier` (private).

## Required Read Order

1. `~/.claude/CLAUDE.md`
2. Project `AGENTS.md` or `CLAUDE.md`
3. This file: `docs/session-sync.md`
4. `ChangeLog.md`
5. `ResearchLog.md`
6. `SMOKE_TESTS.md`
7. The focused official docs and plans linked below

## Current Objective

Build the official P0 optical-microscopy classifier for `Скажи мне, кто твой шлиф` without carrying over the old broad OM/SEM/XRD application surface.

Current target pipeline:

```text
official panorama image
-> binary sulfide segmentation
-> sulfide connected components
-> ordinary_intergrowth / fine_intergrowth classification
-> talc detection and talc fraction
-> deterministic ore class rule
-> mask, overlay, confidence heatmap, metrics, report
```

## Scope Boundaries

- In scope: official OM images, sulfide/non-sulfide segmentation, talc detection, high-resolution overlapping tiling, Streamlit QA, training manifests, model evaluation, final artifacts.
- Out of scope by default: SEM, XRD, defect/product platform UI, old generic QC dashboard, broad mineral ontology.
- Pseudo-labels are weak supervision, not expert geological ground truth.
- Non-expert QA may fix visually obvious mask errors, mark uncertain/excluded areas, and produce training patches.

## Dataset

The local `dataset` entry is a symlink:

```text
dataset -> ../2026_Nornikel_Hackaton/dataset
```

Source dataset facts from the original repository handoff:

- official download complete;
- `dataset/_download_manifest.json` verified `1236/1236` files;
- total verified bytes: `3,018,194,503`;
- panoramas: `14` JPG images, largest around `27025 x 21227` px;
- image-level class folders include ordinary/row, fine/hard-to-process, and talcose ore examples;
- `Области оталькования` contains blue-line talc annotations and must be inspected before training the talc detector.

## Focused Docs

- `presentation.md`: slide-by-slide explanation of the current official OM-only approach for the project presentation.
- `docs/ui/v2/`: canonical folder for v2 UI docs, including ore pipeline UI specs/plans, talc review UI docs, and the v2 UI candidate backlog. Project instructions now direct future agents to keep new v2 UI docs here instead of root `docs/specs/`, `docs/plans/`, or `docs/notes/`.
- `docs/ui/v2/notes/2026-07-03-ore-pipeline-ui-ux-qa-review.md`: live UI/UX QA review of the v2 ore pipeline app. The highest-priority findings have been implemented, and the follow-up live review now records remaining UI gaps: clipped result-layer chips, Edit & Recalculate statistics wrapping/collapsed labels, missing backend/readiness display, missing rule provenance, no evidence bundle, no review-candidate/source-disagreement surface, and confusing talc-review queue semantics.
- `docs/ui/v2/notes/2026-07-03-ore-pipeline-contextual-viewer-controls.md`: current v2 ore pipeline viewer-control contract: segmentation class checkboxes are hidden for image-only views, shown contextually for sulfide/final layers including side-by-side, and viewer-level tiling/contours/opacity controls sit in a separate row below the image canvas.
- `docs/ui/v2/TODO_CANDIDATES.md`: refreshed ranked candidate backlog for the v2 ore pipeline and closely related browser UI work; use it to choose UI follow-ups without reopening the old broad OM/SEM/XRD surface.
- `scripts/generate_augmentation_review_gallery.py`: static HTML gallery generator for visual review of v2 runtime augmentation settings; default output is `outputs/augmentation_review/index.html`.
- `EXTERNAL_DATASETS.md`: teammate-facing list of external datasets to download, expected local paths, verification commands, and datasets not needed for the current v2 run.
- `docs/official/Скажи мне кто твой шлиф.md`: saved official task page.
- `docs/official/Постановка задачи.docx`: official source document copy.
- `docs/specs/official-tz-solution-map.ru.md`: requirement-by-requirement solution mapping.
- `docs/plans/25_standalone-ore-classifier-project.md`: standalone implementation plan.
- `docs/plans/26_weak-supervision-sulfide-binary-model.md`: binary sulfide weak-supervision plan with Streamlit QA.
- `docs/plans/27_talc-silicate-support-labeling.md`: optional silicate-support plan for conservative talc positives and hard negatives.
- `docs/plans/35_talc-detector-training.md`: approved talc detector training plan on the 42 reviewed masks (dataset recipe, DINOv2 baseline -> SegFormer target, augmentation caution, eval protocol with oracle-luma IoU 0.502 as the number to beat); step 1 dataset builder is implemented.
- `docs/ui/v2/plans/28_ore-pipeline-ui.md`: implemented local browser/canvas UI plan for upload, preprocessing, immutable runs, visualization, edit-and-recalculate, exports, and history.
- `docs/ui/v2/specs/ore-pipeline-preprocessing-control-v0.1.md` and `docs/ui/v2/plans/29_ore-pipeline-preprocessing-control.md`: implemented compact v2 preprocessing gate `[x] Preprocessing [Edit...] [Apply]`.
- `docs/ui/v2/specs/ore-pipeline-apply-prepared-run-v0.1.md` and `docs/ui/v2/plans/37_ore-pipeline-apply-prepared-run.md`: v2 contract for pressing Augmentation/Preprocessing `Apply` after a completed run. A new immutable `prepared` run is created with only prerequisite artifacts rebuilt, repeated Apply before Start updates that prepared run, and Start continues it in place.
- `docs/ui/v2/specs/ore-pipeline-status-page-v0.1.md` and `docs/ui/v2/plans/38_ore-pipeline-status-page.md`: v2 `Status` / `Статус` page contract for `/status` and `/api/status`, covering CPU, optional NVIDIA GPU, RAM, Flash, history size, active jobs, backend/checkpoint, and health checks.
- `docs/ui/v2/specs/ore-pipeline-metadata-editor-v0.1.md`: v2 `Edit Metadata...` popup contract for curated domain metadata, raw read-only image metadata, session defaults, scale guardrails, and per-run metadata artifacts.
- `docs/ui/v2/plans/30_ore-pipeline-metadata-editor.md`: v2 metadata editor hardening plan.
- `docs/ui/v2/specs/ore-pipeline-scale-metrics-v0.1.md` and `docs/ui/v2/plans/33_ore-pipeline-scale-metrics.md`: v2 calibrated scale contract for pixel/physical area metrics in `run.json`, the result table, `metrics.csv`, and PDF reports.
- `docs/ui/v2/specs/ore-pipeline-batch-v0.1.md`: v2 grouped multi-image run contract; the user-facing UI now calls this workflow `Series` / `Серии`, while route/storage/API names remain `batch` for compatibility.
- `docs/ui/v2/plans/32_ore-pipeline-batch-ui.md`: v2 implementation plan for `apps/ore_pipeline_web.py` Series support on the existing batch routes/storage.
- `docs/ui/v2/specs/ore-pipeline-system-settings-v0.1.md` and `docs/ui/v2/plans/31_ore-pipeline-system-settings.md`: implemented server-backed Settings page for system-wide app defaults.
- `docs/ui/v2/specs/ore-pipeline-artifact-mask-v0.1.md` and `docs/ui/v2/plans/33_ore-pipeline-artifact-mask.md`: implemented `Artefacts` edit layer for grinding/polishing exclusion masks before and after runs.
- `docs/ui/v2/plans/34_ore-pipeline-augmentation-misses.md`: v2 UI/training plan for the remaining augmentation misses: canonical training/runtime profiles, artifact-mask review/export, reviewed talc-mask attachment, rule/calibration visibility, warning/robustness surfacing, training-feedback export, and reproducible augmentation review evidence.
- `docs/ui/v2/specs/ore-pipeline-docker-runtime-v0.1.md` and `docs/ui/v2/plans/35_ore-pipeline-docker-runtime.md`: Docker/Compose runtime contract for running the v2 ore pipeline GUI on the Nornickel VM as a heuristic-first browser service.
- `docs/ui/v2/notes/2026-07-03-ore-pipeline-docker-vm-smoke.md`: live VM Docker deployment evidence. The image `nornikel/ore-pipeline-ui:v2` was built on `docker-srv`, streamed into `team123@111.88.145.15`, and launched as `nornikel-ore-pipeline-ui` on public port `8080`; normal official-image smokes pass and public `/workspace` averages about `86 ms`, but full panorama start/preparation is currently CPU-bound on synchronous full-size `preprocessed_full.png` generation and should not be used as an unqualified live-demo path until optimized.
- `docs/ui/v2/plans/36_ore-pipeline-source-disagreement-map.md`: plan for the judge-facing `сомнения / disagreement` layer that combines model, heuristic, optional teacher/Petroscope/LumenStone, TTA/ensemble, artifact, ignore, and tile-risk evidence into green/yellow/red/gray maps plus review candidates.
- `docs/benchmarks/01_binary_sulfide_model_benchmark.md`: binary sulfide model benchmark; SegFormer-B2 is the current default checkpoint, SegFormer-B1/B0 remain mirrored fallbacks.
- `docs/cards/binary-sulfide-model-card.md`: model provenance, metrics, limitations, and B0/B1 checkpoint status.
- `docs/cards/official-balanced-eval-dataset-card.md`: balanced image-level eval split and panorama caveats.
- `docs/cards/demo-run-fact-sheet.md`: B1 demo pipeline input, parameters, outputs, and deterministic result.
- `docs/notes/2026-07-03-official-metrics-and-panorama-split.md`: organizer clarification on IoU/Hausdorff, F1/AUC, and unlabelled panorama usage.
- `docs/notes/2026-07-03-research-mindstorm-improvements.ru.md`: research-backed Russian mindstorm for killer features, training upgrades, annotation strategy, and presentation framing.
- `docs/notes/2026-07-03-pipeline-improvement-proposals.md`: current-state-verified pipeline gap review with tiered, deadline-aware proposals (data audit/dedupe, denominator fix, image-level F1 calibration loop, talc ±3pp evaluator, pretrained-init retrain, panorama compliance benchmark) and a suggested execution order.
- `docs/notes/2026-07-03-talc-ground-truth-strategy.md`: talc pixel detection and ground-truth strategy; blue-line annotations are region-level not pixel GT, so the note proposes point-counting fraction GT, PU/enrichment-derived tri-map training labels, an optional dual-annotator silver set, organizer questions, and a detection-model ladder (DINOv2 baseline -> small SegFormer target). Now also records the quantified luma-threshold finding (talc flakes are the darkest phase, no global threshold value) and the completed manual review outcome: all 42 samples have `reviewed/reviewed_talc_mask.png`, a median 83% of reviewed talc lies outside the original blue-line bags (annotations were examples, not exhaustive), reviewed talc fractions are median 0.274, an oracle per-image luma threshold reaches median IoU 0.502 vs reviewed GT, and the production HSV talc candidate scores median IoU 0.000 and must be replaced; ore-rule calibrations that consumed HSV `talc_fraction` need regeneration. Analysis artifacts under `outputs/talc_luma_enrichment/`.
- `docs/notes/2026-07-03-reusable-demo-libraries.md`: shared libraries for source fusion, active-review queues, dataset curation, component reports, report cards, and scribble classifiers.
- `docs/notes/2026-07-03-b1-visual-validation-pack.md`: six-image final-B1 visual sanity pack and calibration finding for ordinary/fine rules.
- `docs/notes/2026-07-03-b2-manual-review-pack.md`: current B2 manual review pack, visual panels, heatmaps, uncertainty crops, feedback template, and Streamlit review command.
- `docs/notes/2026-07-03-heuristic-segmentation-subproject.md`: separate non-neural segmentation baseline, smoke result, limits, and intended use as a disagreement source.
- `docs/notes/2026-07-03-gpu-training-status.md`: current binary sulfide dataset, gx10 ResUNet job, and zelda blocker.
- `docs/ui/v2/notes/talc-blue-line-conversion.md`: v2 talc blue-line converter/review note.
- `docs/ui/v2/specs/talc-mask-review-web-app-v0.1.md`: implemented browser/canvas replacement for the unreliable Streamlit mask-editing UI. `apps/talc_review_web.py` starts directly from the raw `Области оталькования` folder or a prepared workspace, pairs MS Paint annotated images with same-filename originals, creates/reuses the talc conversion workspace, supports a single top toolbar with Brush/Fill/Similar Talc/Rectangle/Polygon/SAM2/Undo/Zoom In/Zoom Out/Fit plus active-tool parameters, `B`/`F` Brush/Fill shortcuts, top-right `Save` and `Save & Next`, mouse-wheel zoom with visible percentage, middle-button drag panning, a non-destructive luma-threshold dark-pixel background preview, brush left-draw/right-erase plus bounded Fill against blue strokes/sulfides/current talc regions/image edges plus Similar Talc seed-patch-anchored luma/color preview/apply against non-sulfide pixels with positive-bag filtering plus polygon/rectangle/SAM2 talc-mask editing, undo, explicit saved/saving/error autosave state, human-readable sample status labels, filtered-queue `Save & Next`, and visible warnings for unavailable selected backgrounds/layers.
- `docs/notes/2026-07-02-domain-datasets-search.md`: official dataset inventory and external dataset context.
- `docs/notes/2026-07-02-targeted-om-datasets-models.md`: targeted OM sulfide/talc dataset/model review.
- `docs/notes/2026-07-02-telegram-shlif-captains-chat.md`: organizer confirmations.

## Immediate Next Steps

1. Monitor zelda sharded B2 batch: `tmux nornickel_v2_b2_fine_intergrowth`, `tmux nornickel_v2_b2_ordinary_intergrowth`, and `tmux nornickel_v2_b2_talcose` are running into `outputs/evaluations/b2_official_deconflicted_auto_talc_analyzed_sharded_20260703_1000/{fine_intergrowth,ordinary_intergrowth,talcose}`. After all shards complete, combine them with `scripts/merge_official_batch_shards.py`, then run `scripts/evaluate_ore_classification.py` and `scripts/calibrate_ore_rules.py` on the combined `summary.csv`.
2. Use the local SegFormer-B2 mirror at `models/binary_sulfide/segformer_b2_dataset_v0_zelda_20260703_overnight_safetensors/` as the default sulfide checkpoint.
3. Keep the local SegFormer-B1 mirror at `models/binary_sulfide/segformer_b1_dataset_v0_zelda_20260703_overnight_safetensors/` as the faster fallback checkpoint.
4. Use `outputs/official_balanced_eval_split_deconflicted.json` for preferred balanced labelled-class evaluation; keep the raw `outputs/official_balanced_eval_split.json` only as a baseline comparison.
5. Pull the completed zelda sharded batch locally and save the evaluation/calibration Markdown beside the combined output.
6. Use `--rule-config-json <.../ore_rule_calibration.json>` for calibrated reruns or demos after selecting a calibration artifact; current visual packs show rule disagreements that should not be hidden.
7. Compare SegFormer-B1/B0 predictions against `heuristic_segmentation/` outputs and surface disagreement areas as the next sulfide QA queue if time permits.
8. Wire the reusable demo libraries into pipeline outputs: `source_fusion` -> `review_queue` -> `component_reports` -> `report_cards`, with `curation` for split/label QA and `scribble_classifier` as an optional reviewer-assist source.
9. Use the research mindstorm note to prioritize the remaining differentiating features: robustness certificate, illumination/flat-field artifact, high-loss pseudo-label cleanup, annotation-budget simulation, OIA-style report protocol, and MIL over sulfide components.
10. Keep `outputs/manual_review/b2_balanced_review_pack/` available as optional QA evidence, but the current implementation path no longer blocks on manual review.
11. Review and accept/fix talc masks with `apps/talc_review_web.py` if the automatic talc candidate is not good enough for demo claims; keep `apps/talc_review_streamlit.py` as the legacy fallback only.
12. Use `COMMANDS.md` for the current preferred talc review launch command and `SMOKE_TESTS.md` for the browser-app smoke checklist.
13. Use `apps/ore_pipeline_web.py` for the requested end-to-end demo UI. Default launch is heuristic backend for smoke reliability; use `--backend ml --checkpoint models/binary_sulfide/segformer_b2_dataset_v0_zelda_20260703_overnight_safetensors/best.pt` when the full ML environment is active.
14. The Docker VM GUI is currently live at `http://111.88.145.15:8080/workspace` as container `nornikel-ore-pipeline-ui` from image `nornikel/ore-pipeline-ui:v2`; see `docs/ui/v2/notes/2026-07-03-ore-pipeline-docker-vm-smoke.md` for build/upload/run/performance details.
15. Before live panorama demos, optimize the Docker/UI upload-preparation path so large panoramas do not synchronously write full-size `preprocessed_full.png` before returning a run ID; the 46 MB `dataset/Панорамы/13.jpg` stress smoke was CPU-bound for several minutes at about `1.54 GiB` RSS.
16. For the next UI pass, start from the refreshed `docs/ui/v2/TODO_CANDIDATES.md` P0 list: result viewer toolbar fit, backend readiness panel, demo sample launcher, rule/calibration provenance, reviewed talc-mask input, evidence bundle, and panorama compliance run card.
17. For the disagreement-map feature, use `docs/ui/v2/plans/36_ore-pipeline-source-disagreement-map.md`: first implement `model mask + heuristic mask + artifact/analyzed mask`, then add teacher/TTA only after the baseline layer is visible and tested.
18. Use `docker compose -f docker-compose.ore-pipeline-ui.yml up --build` for the VM GUI path when a browser-accessible container is needed; it defaults to heuristic backend and persists state in `outputs/ore_pipeline_ui`.
19. Use `docs/ui/v2/plans/34_ore-pipeline-augmentation-misses.md` as the next augmentation hardening plan before claiming training/UI augmentation parity: shared profiles first, then artifact/talc/calibration/warning/export evidence.

## Known Risks

- No geologist is currently available; QA must be framed as non-expert cleanup and uncertainty marking.
- Official class folders are image-level labels, not pixel masks.
- Official panoramas are unannotated and unclassified in the provided dataset; use them for performance, visual QA, and stress testing unless a new annotation/classification pass is created.
- Organizer-recommended production metrics are IoU and Hausdorff distance for segmentation, F1 and AUC for classification; current weak-label IoU benchmarks are incomplete by that standard.
- Talc annotations are drawn as colored lines, so mask extraction needs pairing checks and visual QA.
- Very large panoramas require overlapping tiling and streamed stitching; full-image probability tensors can exceed practical memory.
- Docker VM smoke confirmed normal official images are fine on `team123`, but full panorama upload/start preparation is currently latency-bound by synchronous full-size preprocessing PNG generation before the async run starts. Treat live panorama demos as risky until that path is optimized or pre-staged.
- The ore pipeline UI keeps original files immutable but displays/run-edits on generated preview/analysis-scale artifacts for large images; full-resolution model-grade tiled editing remains a future hardening step.
- Zelda `root@161.104.48.181` initially booted without a visible NVIDIA GPU, then recovered after retry/reboot. Re-check `nvidia-smi` after any restart before assuming CUDA is available.
- The local macOS Python environment may not load zelda-trained SegFormer checkpoints because installed `transformers` can use a different module namespace. For B2 review-pack generation, zelda with `transformers 5.12.1` was used successfully.

## Implemented Binary Sulfide Block

- Official manifest generation works with very large panoramas by disabling the PIL decompression limit; `outputs/official_manifest.json` contains `1236` images.
- Binary sulfide dataset builder writes tiled RGB images, masks, ignore masks, and a JSON manifest under `outputs/binary_sulfide_dataset_v0`.
- Training script supports `resunet`, `segformer_b0`, `segformer_b1`, and `segformer_b2` with ignored pixels, AMP, checkpoints, CSV logs, and IoU metrics.
- `scripts/evaluate_binary_sulfide.py` reports IoU, F1, AUC, Hausdorff, and HD95. SegFormer-B1 best eval: sulfide IoU `0.971548`, F1 `0.985569`, AUC `0.998522`, HD95 mean `26.25 px` on 512 sampled val tiles.
- `scripts/audit_official_labels.py` audits labelled official folders by SHA-256. Current audit: `1180` labelled images, `1124` unique hashes, `56` duplicate-content groups, `24` label-conflict groups, and `48` conflict paths.
- Preferred eval split is `outputs/official_balanced_eval_split_deconflicted.json`: `345` labelled images (`115` ordinary, `115` fine, `115` talcose) after excluding conflicting-label hashes and duplicate content. Raw split remains `outputs/official_balanced_eval_split.json` with `387` images.
- `src/ore_classifier/analyzed_area.py` provides the shared analyzed-area mask used to exclude black borders and blue annotation strokes from fraction denominators.
- `scripts/infer_binary_sulfide.py` runs overlapping tiled inference and writes `sulfide_mask.png`, `confidence.png`, `analyzed_mask.png`, `overlay_preview.jpg`, and `summary.json`.
- `scripts/analyze_ore_from_masks.py` computes connected-component ordinary/fine features, ore class rule output, `component_features.csv`, `analyzed_mask.png`, and intergrowth overlay. It accepts `--rule-config-json` plus explicit threshold overrides. Component morphology is cropped to each padded connected-component bounding box to avoid repeated full-frame morphology on large images.
- ML pipeline fraction fields now use analyzed non-excluded pixels by default: `sulfide_fraction` and `talc_fraction` are analyzed-denominator values, while `sulfide_fraction_image` and `talc_fraction_image` preserve full-image denominators.
- `ore_summary.json` includes `talc_margin`, `intergrowth_margin`, `needs_expert_review`, and `warnings` so near-threshold and zero-sulfide cases are explicit.
- `src/ore_classifier/talc_candidate.py` provides a conservative automatic talc candidate mask from optical RGB plus sulfide exclusion. This is a runtime candidate, not expert talc ground truth.
- `scripts/run_ore_pipeline.py` runs image -> sulfide mask -> optional provided/automatic talc mask -> ore summary in one command. Use `--auto-talc-candidate` for the current no-manual-review path, or `--talc-mask` for accepted masks. It accepts `--rule-config-json` for calibrated demos.
- `scripts/run_official_batch.py` runs the full pipeline over the raw or deconflicted balanced split and writes `summary.csv/json` with source labels, predicted ore class, fractions, applied rule thresholds, artifact paths, and failures. It passes `--rule-config-json` through to each image run.
- `scripts/evaluate_ore_classification.py` computes image-level accuracy, per-class precision/recall/F1, macro/weighted F1, confusion matrix, and one-vs-rest AUC from the batch `summary.csv`.
- `scripts/evaluate_ore_feature_classifier.py` cross-validates image-level classifiers from batch summary fractions plus per-run component aggregate features. Current 345-image B2 deconflicted split result: ExtraTrees macro F1 `0.7439`, weighted F1 `0.7439`, accuracy `0.7420`, macro AUC OVR `0.8802`.
- `scripts/merge_official_batch_shards.py` combines class-sharded official batch outputs into one `summary.csv/json` plus `failures.json` and rejects duplicate `run_id` values.
- `scripts/calibrate_ore_rules.py` grid-searches deterministic ordinary/fine/talc thresholds from a completed batch `summary.csv` plus per-run `component_features.csv`, writes `ore_rule_calibration.json/md`, and keeps the calibration artifact explicit because it uses image-level folder labels, not pixel-level geological ground truth. The resulting artifact can be passed back into `run_ore_pipeline.py` or `run_official_batch.py` with `--rule-config-json`.
- `scripts/build_official_balanced_eval_split.py` can generate both the raw balanced split and the deconflicted split when passed `--label-audit-json --exclude-conflicts --dedupe-sha256`; panoramas are listed separately as unlabelled.
- Final B2 demo output exists under `outputs/inference_demo/b2_final_row_2539589_1/`: final B2 inference on official row ore image, sulfide fraction `0.296259`, component summary, confidence map, and overlays.
- B2 manual review pack exists under `outputs/manual_review/b2_balanced_review_pack/`: 9 balanced official-class samples, `review_panel.jpg` per run, source previews, sulfide overlays, confidence heatmaps, ordinary/fine overlays, `8` uncertainty crop candidates, `review_manifest.csv/json`, `review_candidates.csv`, and `feedback_template.csv`. Source subset copy exists under `outputs/manual_review/source_dataset_subset/`.
- Manual review pack generator: `scripts/prepare_manual_review_pack.py`. The Streamlit sulfide QA app now displays optional `review_panel`, `source_preview`, and `confidence_heatmap` paths when present. It also shows the run source image and inferred source `dataset/...` path for the original image, using `review_manifest.csv` when available.
- Auto-talc full-path smoke output exists under `outputs/commit_smoke_ore_pipeline_auto_talc/`, and one-image official batch/eval smoke output exists under `outputs/commit_smoke_official_batch/`.
- Local smoke tests passed for ResUNet and SegFormer-B0 on `outputs/smoke_binary_sulfide_dataset`; full local unit tests now cover `63` tests.
- gx10 ResUNet training completed 30 epochs and is mirrored locally.
- zelda SegFormer-B2 training completed 30 epochs; best validation sulfide IoU is `0.974381` at epoch 20, with final epoch 30 IoU `0.969119`.
- zelda SegFormer-B1 training completed 30 epochs; best validation sulfide IoU is `0.971548` at epoch 16, with final epoch 30 IoU `0.964032`.
- gx10 ResUNet training completed 30 epochs; best validation sulfide IoU is `0.956436` at epoch 26, with final epoch 30 IoU `0.953216`.
- zelda SegFormer-B0 training completed 30 epochs; current best validation sulfide IoU is `0.953371` at epoch 13, with final epoch 30 IoU `0.951119`.
- zelda SegFormer-B2/B1/B0 and gx10 ResUNet `best.pt`, `last.pt`, `train_log.csv`, and `metrics.json` were mirrored locally under `models/binary_sulfide/`.
- Current binary sulfide benchmark is saved in `docs/benchmarks/01_binary_sulfide_model_benchmark.md`.

## Implemented Heuristic Block

- Separate subproject lives under `heuristic_segmentation/` and does not modify the neural training path.
- CLI: `python3 heuristic_segmentation/run_heuristic_segmentation.py --image ... --output-dir ...`.
- Outputs: `class_mask.png`, `sulfide_mask.png`, `talc_candidate_mask.png`, `analyzed_mask.png`, `overlay.png`, `components.csv`, `metrics.json`, `run_summary.json`, and `batch_summary.json`.
- Current method: analyzed-area mask, illumination-normalized brightness threshold, green/blue artifact suppression, morphology, connected components, ordinary/fine rules from area/solidity/compactness/replacement ratio, and conservative `talc_candidate`.
- Smoke on `dataset/Фото руд по сортам. ч1/Рядовые руды/DSCN2176.JPG` at `--max-side 900` passed; metrics were `sulfide_fraction 0.164864`, `talc_candidate_fraction 0.000708`, `component_count 70`.
- Unit tests: `python3 -m unittest discover -s heuristic_segmentation/tests -p 'test_*.py' -v`.

## Implemented Ore Pipeline UI

- `apps/ore_pipeline_web.py` is a local `http.server` app with generated HTML/CSS/vanilla JS canvas for the requested end-to-end UI.
- The ore pipeline GUI also has a Docker/Compose runtime for VM demos: `docker/ore-pipeline-ui/Dockerfile`, `docker/ore-pipeline-ui/entrypoint.sh`, `docker/ore-pipeline-ui/requirements.txt`, and `docker-compose.ore-pipeline-ui.yml`. The image binds `0.0.0.0:8080`, defaults to heuristic backend, stores persistent UI state in `/data/ore_pipeline_ui`, mounts `outputs/ore_pipeline_ui`, and deliberately excludes `dataset`, `outputs`, and checkpoint files from the build context.
- Upload supports click-to-open and drag/drop for PNG, JPEG, TIFF, and RAW-extension files. Unsupported dropped/selected extensions show an inline localized warning before upload. Large uploads show byte-transfer progress followed by preview-preparation progress only in the input-panel progress row while the server decodes and scales display pyramids; the Run progress bar is reserved for `Start`/active run polling. Manual preprocessing preview updates use the same input-panel progress row. RAW decoding uses optional `rawpy`; regular formats use Pillow.
- After an image is selected, the upload drop zone shows a thumbnail, filename, dimensions, and an `×` clear button. Clearing resets the frontend upload/run/results/progress/view state to the initial page while preserving immutable run history.
- The v2 ore pipeline UI now has a run-level metadata editor for the selected image, specified in `docs/ui/v2/specs/ore-pipeline-metadata-editor-v0.1.md` with hardening plan `docs/ui/v2/plans/30_ore-pipeline-metadata-editor.md`: `Редактировать метаданные...` / `Edit Metadata...` opens Domain, Raw, and Session Defaults tabs; Domain starts with `Session specific` fields such as Project, Microscope/camera, Objective, scale source, scale value, and scale confidence, then `Sample specific` fields; raw upload/header metadata is read-only; browser session defaults can fill repeated fields; scale handling is conservative and warns when the scale value is set without calibrated scale; `exclude_from_training` is labelled as excluding this image from training/validation dataset exports; saved metadata is sent on `Start` and written as `metadata/curated_metadata.json` inside each immutable run. The left sidebar keeps only the metadata action button, without a persistent helper/status sentence. Edit-and-recalculate derived runs inherit the parent `curated_metadata`.
- The app stores original upload artifacts under `outputs/ore_pipeline_ui/uploads/` and immutable runs under `outputs/ore_pipeline_ui/runs/`.
- Pressing `Apply` in Augmentation or Preprocessing while a completed run is loaded creates a new immutable `prepared` run instead of mutating the completed run. The prepared run keeps original/prerequisite artifacts up to the changed step, preserves compatible artefact masks, clears downstream sulfide/final masks plus metrics/reports, and is continued in place by `Start`; pressing `Apply` again before `Start` updates the same prepared run rather than creating another history entry.
- The v2 ore pipeline UI now has a user-facing `Series` / `Серии` page in `apps/ore_pipeline_web.py`. The technical route remains `/batch`: `/batch` creates/opens a grouped gallery, `/batch/{batch_id}` reopens persisted state from `outputs/ore_pipeline_ui/batches/{batch_id}/batch_summary.json`, `Add images` lives inside the Gallery panel and accepts multiple uploads, draft item cards have `Edit Metadata...` plus `Remove`, shared preprocessing and runtime augmentation settings are captured at `Run Series`, items run sequentially as ordinary immutable runs, per-card progress is retained, `reports/batch_results.csv` is written on completion, and each item keeps `Load` to open the child run results with `Back to Series` return context.
- Display layers use generated preview pyramids for original, augmented, preprocessed, sulfide overlay, and final class overlays so large images are not pushed into the browser at full resolution. Scaled/large-image preprocessing records a tile manifest that the `show tiling` viewer checkbox can draw over original, augmented, preprocessed, sulfide, final, and side-by-side views.
- Preprocessing is now controlled by compact `[x] Предобработка [Настроить...] [Применить]` / `[x] Preprocessing [Edit...] [Apply]` controls. The four detailed settings live in a popup: illumination normalization, denoising, contrast correction, and panorama scaling. Each popup setting has a localized `(?)` hover/focus hint with a short operational description. Panorama scaling is explicit: enabled/off plus either a longest-side pixel bound or a scale factor, with the active rule shown in summaries and recorded in run metadata. Turning panorama scaling off falls back to the normal processing max side and does not disable the independent tiling manifest. The main checkbox defaults to enabled and persists with the detailed settings through the server-backed Settings page, with browser local storage as fallback; on the Settings page, panorama scaling is visually separated into its own scale group below a divider from the main preprocessing defaults. `Apply` checks preprocessing and refreshes the preprocessed preview for not-yet-started uploads; on completed runs it creates or updates a `prepared` run up to the preprocessing step. `Start` with preprocessing unchecked records `preprocessing_enabled=false`, runs on an analysis-scale original internally, and does not expose a user-facing preprocessed display layer, so the preprocessed primary/side-by-side options stay disabled.
- Runtime augmentation is implemented in `apps/ore_pipeline_web.py` using reusable `src/ore_classifier/augmentation.py`: the left/input panel has `[ ] Augmentation [Edit] [Apply]`, the popup groups color/tone, acquisition-noise, and grinding/polishing artifact settings (scratches, polishing haze, pits/dust specks), settings persist in browser local storage, `Apply` checks augmentation, saves settings, refreshes upload debug previews for not-yet-started uploads, and switches the viewer to `augmented`; on completed runs it creates or updates a `prepared` run up to the augmentation step. Enabled runs create full-source-size `augmentation/augmented.png` for upload previews and immutable `input/augmented.png` in run artifacts, preprocessing consumes the full-size augmented image, and run metadata records the exact settings/artifact paths.
- `scripts/generate_augmentation_review_gallery.py` creates `outputs/augmentation_review/index.html` for augmentation review: one deconflicted official sample per class by default, original plus eight deterministic augmentation variants, local preview images, and exact settings JSON per card.
- Result views cover primary original, augmented, preprocessed, sulfide/non-sulfide, and final segmentation layers. `augmented` appears between `original` and `preprocessed`; augmentation and preprocessing are applied at original source dimensions first, then `preprocessed.png` is downscaled to analysis/display size according to panorama scaling, while preprocessing-enabled runs keep `input/preprocessed_full.png`. Artefact regions render inside the sulfide and final overlays when the relevant artefact visibility checkbox is enabled, using the distinct violet/magenta grinding/polishing exclusion color; layers stay disabled/greyed until their artifacts exist; and side-by-side is a separate none/augmented/preprocessed/sulfide/final comparison selector with the selected layer on the right and a dedicated DOM-overlay draggable splitter, without a decorative arrow divider before the label. The viewer toolbar segmented selectors keep their items on one line and do not show horizontal scrollbars. Segmentation class visibility legends are contextual overlays on the image view: primary/left layer controls appear in the top-left corner, side-by-side/right layer controls appear in the top-right corner, each visible legend is a vertical class list, image-only layers hide them, sulfide shows only sulfides/non-sulfides/artefacts, and final shows only ordinary/fine/talc/artefacts/background. `show tiling`, `contours only`, and `opacity` are viewer-level controls in one row below the image canvas.
- Page routing uses slug URLs: `/` redirects to `/workspace`; `/workspace`, `/batch`, `/batch/{batch_id}`, `/history`, `/history_series`, `/settings`, and `/status` are direct-loadable; selecting Series mode on the History page pushes `/history_series`; tab clicks update the URL; and browser back/forward restores the selected page/history mode.
- The v2 ore pipeline UI has a read-only `Status` / `Статус` page at `/status`. `GET /api/status` reports app uptime, backend/checkpoint state, CPU load from `os.getloadavg`, optional NVIDIA GPU utilization/memory/temperature from `nvidia-smi`, RAM, Flash/disk usage for the workspace, history size, upload size, run/series counts, active jobs, and health checks with overall `ok`/`warning`/`error`. The page uses explicit `Refresh` instead of background polling and hides the workflow sidebar.
- The UI has a persistent `Theme` selector with `System`, `Light`, and `Dark` modes using theme-aware panels, controls, toolbar, modal, history rows, and viewer backgrounds.
- The UI has a persistent `Language` selector and defaults to Russian (`Русский`); static controls, preprocessing labels, upload/progress/history/editor messages, result summaries, and metric labels are localized in Russian and English without mutating stored run metadata.
- The UI has a server-backed Settings page at `/settings`. `GET/PUT /api/settings` persist system-wide defaults in `outputs/ore_pipeline_ui/settings/app_settings.json`: language, theme, preprocessing defaults, default tiling overlay, and repeated session metadata defaults. Browser local storage remains a fallback for existing sessions.
- `Start` creates a new run and reports staged progress/ETA. While a run is queued/running, `Start` is replaced by a red `Stop` button; `Stop` calls `POST /api/runs/{run_id}/cancel`, the worker cooperatively records terminal `canceled` status, and ML subprocess runs are terminated before the UI re-enables tuning. Default backend is heuristic for local smoke reliability; optional `--backend ml --checkpoint ...` calls the existing ML pipeline.
- Metrics include analyzed area, total sulfide fraction, ordinary/fine intergrowth fractions, talc fraction, other analyzed area, image artefact fraction, and component count. The result panel is vertical: text output first, then a full-width hierarchical metrics table. Rows with area semantics carry `area_px`; when curated metadata supplies a calibrated positive `microns_per_pixel` or `pixel_size_um` plus scale source/confidence, the UI table and `reports/metrics.csv` also include physical `area_um2`/`area_mm2` with scale provenance. The result panel labels denominators explicitly and shows a decision-rationale sentence with sulfide/talc shares and rule margins. CSV and PDF report exports are generated per run; `/api/runs/{run_id}/report.pdf` regenerates a five-page A4 demonstration report shaped by the public ore/lab report style note in the original repo: `Паспорт исследования`, conclusion, `Результаты количественного анализа`, original/preprocessed photo-documentation with preprocessing details, two-color sulfide/non-sulfide map, full final class segmentation, separate side-by-side final-overlay/mask rows for ordinary intergrowths, fine intergrowths, and talc, plus method/QC limitations/reproducibility/expert-review fields. The same export row includes `View files`, which opens a popup listing immutable run files with byte sizes and `WxH` image dimensions, plus `Download ZIP` for `/api/runs/{run_id}/artifacts.zip`. Run metadata JSON writes are atomic so async `/api/runs/start` responses can safely read payloads while worker progress updates are being persisted.
- The left-sidebar History cards show a small thumbnail on the left with `Load` directly underneath it, leaving the rest of the card for filename, run id, date, and localized classification text. The History page has three modes: all runs, standalone single runs, and series. The run modes use a table with thumbnail, filename, date, ore classification, sulfide/non-sulfide and final-class fractions, plus `Load` and `Remove` actions; standalone mode excludes child runs created by Series. Series mode uses the `/history_series` slug, lists persisted grouped runs from the existing batch store, and exposes `Open` plus `Remove`; `Open` opens `/batch/{batch_id}` for details, while `Remove` calls `DELETE /api/batches/{batch_id}` and deletes the persisted series folder plus child immutable runs after rejecting active series. Clicking a run thumbnail opens a modal preview from the run preview pyramid. Run-level `Load` restores the original upload and preprocessing preset for tuning; run-level `Remove` deletes the selected completed/failed run artifact from history through `DELETE /api/runs/{run_id}` while leaving uploads intact.
- `Fix me` is available after image upload, before `Start`, to mark grinding/polishing artefacts. The Edit & Recalculate dialog has `Artefacts`, sulfide/non-sulfide, and final segmentation tabs; before a completed run exists only `Artefacts` is enabled and saves an upload-level mask for the next run. After a run completes, artefact edits create immutable derived runs, and marked pixels are excluded from sulfide, talc, final masks, and analyzed-area metric denominators. The editor uses the same themed violet/magenta artefact color as the main viewer, a top toolbar for Brush/Pan, undo/redo, zoom in/out, fit view, and brush size, plus live pixel/% statistics. Sulfide edits recalculate final segmentation, while final edits recalculate metrics/report without replacing the parent sulfide mask.
- `tests/test_augmentation.py` covers deterministic geometry-preserving grinding/polishing artifact rendering, and `tests/test_ore_pipeline_web.py` covers upload, runtime augmentation before preprocessing, preprocessing, full run, CSV/PDF artifacts, rich PDF report page construction, PDF text wrapping, async start/run-json race protection, batch sequential runs with per-item metadata, sulfide edit rerun, final edit recalculation, history, and required UI controls.

## Implemented Reusable Demo Libraries

- `src/ore_classifier/source_fusion.py` fuses heuristic/model/SAM/manual masks, writes weighted probabilities, positive vote counts, fused masks, disagreement maps, and source-agreement summaries.
- `src/ore_classifier/review_queue.py` turns uncertainty, decision impact, and novelty maps into ranked review candidates and Russian expert-question prompts.
- `src/ore_classifier/curation.py` provides lightweight image uniqueness, near-duplicate, hardness, and segmentation label-issue helpers without adding FiftyOne/cleanlab dependencies.
- `src/ore_classifier/component_reports.py` adds association contacts, sulfide liberation proxies, and deterministic ore-decision margin flags.
- `src/ore_classifier/report_cards.py` renders model cards, dataset cards, and run fact sheets for reproducibility/provenance outputs.
- `src/ore_classifier/scribble_classifier.py` provides an ilastik/Labkit-style nearest-centroid pixel classifier from sparse foreground/background scribbles.
- Full local unit tests now cover `45` tests with `python3 -m unittest discover -s tests -p 'test_*.py' -v`.

## Implemented Talc Block

- All 42 blue-line samples have human-reviewed masks under `outputs/talc_blue_line_conversion/samples/*/reviewed/` (`reviewed_talc_mask.png` + `reviewed_ignore_mask.png`), created with `apps/talc_review_web.py`. Analysis vs these masks: median 83% of reviewed talc lies outside the original blue-line bags, oracle per-image luma threshold reaches median IoU 0.502, the production HSV candidate scores 0.000 (see `docs/notes/2026-07-03-talc-ground-truth-strategy.md` and `outputs/talc_luma_enrichment/`).
- `scripts/build_talc_dataset.py` builds the tiled talc training dataset from the reviewed masks in the sulfide-manifest format (consumable by `scripts/train_binary_sulfide.py` unchanged): per-image stratified splits, reviewed-talc-authoritative ignore handling, optional deduplicated pure-negative sources (disabled by default). Current artifact: `outputs/talc_dataset_v0` with 1029 tiles (770 train / 259 val across 10 val images). Tests: `tests/test_build_talc_dataset.py`. Plan: `docs/plans/35_talc-detector-training.md`.
- Code now lives in the v2 layout: `src/ore_classifier/talc_blue_line_converter.py`, `src/ore_classifier/sam2_region_assist.py`, `scripts/convert_talc_blue_lines.py`, `apps/talc_review_web.py`, legacy `apps/talc_review_streamlit.py`, `tests/test_talc_blue_line_converter.py`, and `tests/test_talc_review_web.py`.
- Preferred talc review UI is now `apps/talc_review_web.py`: a local `http.server` app with generated HTML/CSS/vanilla JS canvas. It can start from the raw annotated `Области оталькования` folder or `outputs/talc_blue_line_conversion`, pairs clean originals by exact filename, auto-creates or recovers `current_talc_mask.png` on sample open, serializes concurrent first-open current-mask creation to avoid transient `500` responses under parallel loads, edits the talc mask directly, supports background modes for original/MS Paint/QA/mask-only/sulfide-mask views, supports a non-destructive luma-threshold dark-pixel preview slider with `255` off and `90` quick preset, supports a single responsive toolbar ordered Brush/Fill/Similar Talc/Rectangle/Polygon/SAM2/Undo/Zoom In/Zoom Out/Fit with active-tool parameters plus `B`/`F` Brush/Fill shortcuts and top-right `Save` and `Save & Next` actions, supports cursor-anchored mouse-wheel zoom with a visible percentage and middle-button drag panning, supports brush left-draw/right-erase with a hover-radius circle, bounded Fill against raw/closed blue strokes, sulfide pixels, current talc regions, and image edges, Similar Talc seed-patch-anchored luma/color preview over non-sulfide pixels with Strictness, positive-bag filtering, explicit `Apply Similar`, and automatic active-preview application before `Save`/`Save & Next`, polygon, rectangle, and SAM2 canvas tools plus undo/autosave, uses human-readable sample statuses and explicit working-mask saved/saving/error state, advances `Save & Next` through the active visible queue, warns before abandoning unfinished or failed local edits, shows warnings for unavailable selected backgrounds/layers, supports persistent `System`/`Light`/`Dark` themes, has polygon click-to-add and close-by-clicking-first-point, right-click point removal, right-click draft cancel away from points, rectangle drag or two-corner drawing, editable completed polygon/rectangle regions with point/edge/corner/whole-shape drag, Delete/Backspace selected-shape removal, live filled-mask updates, save-time shape flattening, default `SAM2 box` prompting, dashed SAM2 hover/drag prompt previews, idle `SAM2 point` hover masks shown as non-destructive orange previews with explicit `Apply SAM2`, SAM2 box/polygon prompt clipping plus oversized-mask rejection, default-on protection against new sulfide overlap plus manual sulfide subtraction, and writes reviewed masks, overlay, edit patch, and summary under each sample's `reviewed/` directory.
- Talc review performance check on 2026-07-03: `/` and `/api/manifest` respond at sub-millisecond median locally, `/api/samples` across all 42 samples is about `84 ms` p50 / `96 ms` p95 after current masks exist, and the largest initial per-sample local asset group is about `8 MiB` (`qa_overlay` is the dominant asset). Remaining client-side hotspot to optimize if brush/shape editing feels laggy: pointer-move mask previews rebuild/tint/count full `2272 x 1704` masks in JavaScript.
- `tests/test_talc_review_web.py` covers annotated/original pairing, first-open current-mask creation, unreadable current-mask recovery, reviewed save artifacts, reset behavior, theme/sulfide-guard/brush/fill controls, and HTTP JSON endpoints; `tests/test_sam2_region_assist.py` covers SAM2 prompt clipping and oversized-mask rejection. Local `python3 -m unittest discover -s tests -p 'test_*.py' -v` covers `78` tests.
- Presentation framing now treats missing LumenStone/Petroscope silicate/matrix support on a talc candidate as a hard-negative or uncertainty signal, not as direct talc supervision.
- The talc converter now accepts optional `--silicate-mask-dir` masks by image stem. With support masks, `final_talc_mask` is the supported candidate, unsupported candidate pixels are added to `ignore_mask`, `talc_positive_core_mask` gives conservative positives, and `silicate_hard_negative_mask` gives `not_talc` hard negatives outside the annotation.
- Full conversion of `dataset/Фото руд по сортам. ч1/Оталькованные руды/Области оталькования` was regenerated under `outputs/talc_blue_line_conversion`.
- Manifest counts: `42` samples, `31` `candidate_ok`, `9` `needs_manual_review`, `2` `sulfide_overlap_review_required`.
- Streamlit talc review displays original blue annotation lines explicitly but edits the current talc/ignore mask, not the blue strokes. The main `Workspace` is `Review canvas`, which defaults to `Current mask` and has five clear tools: `Brush`, `Erase`, `Filled polygon`, `Filled box`, and `SAM2 assist`.
- Full filled-area editing is implemented through the local Streamlit component at `apps/components/mask_shape_editor/index.html`. In `Review canvas`, polygon vertices can be dragged, inserted, and deleted; boxes support corner/edge drag before applying the filled area as a mask.
- UX check on 2026-07-03 replaced rerun-resetting `st.tabs` with a stateful `Workspace` segmented control, added local `Current talc px` / `Current ignore px` / `Unsaved edits` metrics inside edit workspaces, and changed edit actions to rerun with flash messages so overlays/metrics update immediately. `Reload base masks` now also clears unsaved edit history and canvas/shape drafts.
- `Advanced` keeps exact-coordinate fallbacks out of the normal review path: polygon table, rectangle form, and coordinate SAM2 prompt. SAM2 remains optional, is available as `SAM2 assist` in `Review canvas`, and requires local `torch` plus the official `facebookresearch/sam2` package.
