# Models

Do not download Hugging Face models into this directory.

Use the shared default Hugging Face cache (`~/.cache/huggingface/`) and refer to models by ID in configs/scripts. Store only lightweight config files, model cards, or external checkpoint pointers here.
