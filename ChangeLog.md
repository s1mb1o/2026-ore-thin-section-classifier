# ChangeLog

## 2026-07-05

- Fixed two `tests/test_ore_pipeline_rest_api.py` cases (`test_health_settings_and_error_contracts`, `test_upload_run_and_artifact_download_endpoints`) that failed on machines without a fast GPU. The REST-contract test store omitted `talc_backend`, so it inherited `DEFAULT_TALC_BACKEND="ml"` (SegFormer-B0, present on disk); the ML talc probe/inference takes ~20s+ on CPU/MPS while the test client uses 10s timeouts, so it timed out (server-side the work still succeeded). Pinned `talc_backend="heuristic"` + `grain_backend="heuristic"` in the store, matching `OrePipelineWebTest`, so the API-contract tests no longer depend on ML model speed. Suite now passes in ~7s.

- **Removed all references to other teams' hackathon projects from the v2 submission repo** (repo is going public, per QA#4). **Moved** two external-comparison notes and the private working scratchpad `presentation/NOTES.md` (which also held a plaintext reviewer password) out to the private legacy repo `/2026_Nornikel_Hackaton/docs/notes/`. **Stripped** the third-party attribution from our own work in-place across the jury deck (`presentation/presentation_ru.md` + re-mirrored `presentation.html`), `DEMO_SCRIPT.md`, plans 36/37/39, specs (grain-HITL, phase-aware, resident-batch), notes (talc-grade-signal, grade-cnn-robustness, region-intergrowth-sota, consolidated-metrics), scripts (`train_grade_classifier.py`, `evaluate_grade_branch.py`, `aggregate_grade_from_grains.py`), and the historical `ChangeLog.md` + `docs/session-sync.md` entries. Our own **Grade-CNN (path A)** model and all its metrics are kept — only the external attribution/comparisons were removed; external datasets we legitimately use (LumenStone/Petroscope) were untouched. Verified clean via full-repo grep; the three edited scripts still `py_compile`. **Security follow-up flagged:** the moved scratchpad had the live reviewer basic-auth password in cleartext — rotate it if that repo is ever shared.

- Prepared the **final reviewer-facing submission documentation package** (Russian, per the QA#4 ruling that the jury/presentation are Russian and repos should be public): rewrote `README.md` to a skim-first reviewer entry (task → talc → `> 10%` rule + deployed URL + review path in the first screen) and added `SUBMISSION_README.md` (access table with live URL `https://nornickel-ai-hackathon.alola.ru/`; reviewer credentials intentionally kept OUT of the public repo — pointed to the presentation «Ссылки» slide, ТЗ-requirements checklist, criteria-compliance table), `QUICKSTART.md` (three launch paths — deployed demo / Docker CPU+GPU via existing `compose.yaml` / host Python — plus honest checkpoint-availability note), `CODE_REVIEW.md` (code map with real `file:line` anchors, e.g. official rule at `component_analysis.py:134`), `EVALUATION.md` (source-tagged metrics + official-criteria verdicts consolidated from `docs/notes/2026-07-05-consolidated-metrics.md`), `MODEL_CARD.md` (per-model provenance + committed-vs-local checkpoint table), `DATA_CARD.md` (dataset provenance, weak-supervision, splits, dedup audit), and `LIMITATIONS.md` (honest gaps by data/metrics/models/performance/scope). No new Dockerfile — the docs reuse the existing `docker/ore-pipeline-ui/Dockerfile[.gx10-ml]` via `compose.yaml`. Fact-checked all eight docs with an adversarial verification workflow across four dimensions (paths/commands, metric fidelity, forbidden-claims per the README-writing rules, cross-links/consistency); the only confirmed error — a wrong `talc_review_web.py:2784` "Similar" anchor inherited from the deck — was repointed to the real color-signature code (`collectSimilarSeedSamples:5017` / `similarStats:5079` / `computeSimilarTalcPreview:5220`).

- Ran a live ore-pipeline UI E2E/default-backend smoke on `127.0.0.1:63589` and recorded it in `docs/ui/v2/notes/2026-07-05-ore-ui-e2e-default-backend-smoke.md`. Findings: the service must run on Python 3.10+ because the ML subprocess uses `zip(..., strict=True)`; after restarting with Python 3.14, default runs completed with ML sulfide + ML talc + heuristic grain classification. Grade-CNN is not used by default, but a per-run `grain_backend=ml` override loads EfficientNet-B3 and records fused Grade-CNN verdicts. On three class-folder smoke images, defaults matched 2/3 and Grade-CNN override matched 2/3.

- Grade-CNN **backbone sweep** to justify the architecture choice empirically. Added `--backbone {efficientnet_b3,convnext_tiny,resnet50}` (torchvision) to `scripts/train_grade_classifier.py` and made `scripts/evaluate_grade_branch.py` arch-aware (reads `arch` from checkpoint). Trained convnext_tiny + resnet50 on gx10 (raw recipe, same leak-free split; efficientnet_b3 raw reused). Held-out 230 macro-F1: **convnext_tiny 0.939**, efficientnet_b3 0.930, resnet50 0.922 — all ≥90%, ~2 pp spread. Conclusion: architecture is not decisive at ~755 training images; efficientnet_b3 (chosen for data-efficiency + no new dep) is confirmed competitive, convnext_tiny a marginal upgrade (= our pp-aware efficientnet_b3 at 0.939). Recorded in `docs/notes/2026-07-05-consolidated-metrics.md` §2.

- Documented our **operational definitions** (talc / ordinary↔fine intergrowth / sulfide + the >10% talcose rule and fusion) explicitly in `README.md` (new "Наши определения" section) and the jury deck (`presentation/presentation_ru.md` slide 8 callout, `presentation.html` re-rendered), per the QA#4 ruling that solutions are graded against the team's own stated definition. This turns the missing talc-fraction GT into a documented, defensible choice.

- Anchored the QA#4 talc-fraction fact to the verbatim transcript (`docs/notes/2026-07-03-official-metrics-and-panorama-split.md`): our question [29:44] and the expert's answer [30:46–31:15] confirm no expert talc-fraction GT exists ("всё, что есть, вам показали, дополнительно ничего не можем уточнить"). Also recorded the **jury ruling** [28:04 / 43:57–44:05 / 46:21]: solutions are graded against the **team's own documented definition** of talc/оталькованная/intergrowth types, with the **10% threshold kept**. Action item: state our operational definitions in the README/deck (converts "no talc GT" into a documented, defensible choice). Reframed compliance in `docs/notes/2026-07-05-consolidated-metrics.md` §5.
- Built and measured the **fused 3-class ore verdict** (`scripts/eval_grade_fusion.py`): talcose ← B0 talc branch, ordinary↔fine ← Grade-CNN (pp-aware), morphology rule as fallback. On the leak-free 345 split (Grade-CNN trained excluding all 345): **macro-F1 0.861** (row 0.867 / fine 0.865 / talc 0.851), acc 0.861 — vs pure-rule 0.508. 211/345 decided by the CNN, 134 by the talc branch. Deployable full 3-class result; the standalone 2-class Grade-CNN (0.93) carries the "тип срастаний ≥90%" criterion (inside the fusion the talc branch's FPs pull row/fine to ~0.865). Artifacts `outputs/evaluations/grade_fusion_20260705.*`; recorded in `docs/notes/2026-07-05-consolidated-metrics.md` §2.
- Downloaded and transcribed Q&A session #4 (`https://disk.360.yandex.ru/i/Oil9NwaW_1MeTw`), verified the video SHA-256 (`a1df6a549d189bd1ecf6b30a7fda96144bc27be2996a7d7b49eb5945ab5066b7`), and saved the Whisper `small` Russian transcript as `docs/official/2026-07-04-qna-session-4-transcript.md`. Updated `docs/official/QnA_sessions.md` with QA#4 source/timestamp-backed answers on public GitHub repos, CPU/GPU expectations, README/Docker/checking workflow, presentation/video links, deadline-window scope, keeping the 10% talc threshold, and explicitly documenting talc/intergrowth methodology.

- Added an image **decode-bomb detector** on the upload path, closing the critical DoS documented in `docs/benchmarks/06_production_load_stability_20260704.md`. New default-on setting `detect_decode_bomb` (Settings → Security, `[x] Обнаруживать «декомпрессионные бомбы»`). Before any full-resolution decode, `_register_upload_file` reads the header-declared dimensions/mode and runs `describe_decode_bomb()`: rejects images over `DECODE_BOMB_MAX_MEGAPIXELS` (1000 MP) or whose decoded-to-file expansion exceeds `DECODE_BOMB_MAX_EXPANSION_RATIO` (300×) on a large raster. On a hit the upload is refused with HTTP 413 and a machine-readable `code: "decode_bomb"` (ApiError now carries an optional `code`, surfaced via a new `_send_api_error` helper); the UI shows a localized message telling the user they can disable the check in Settings. The 1000 MP cap was set by scanning the whole dataset for false positives: single thin sections top out at ~26 MP but stitched panoramas reach ~574 MP (`dataset/Панорамы/16.jpg`) at only ~8× expansion, so the cap sits above the largest real panorama and well below the demonstrated ~2116 MP bomb. Verified live: a 46000×46000 / 6 MB PNG is rejected in ~1 s before decoding, all 1236 dataset images pass, and real images upload normally. Covered by 5 new tests in `tests/test_ore_pipeline_web.py`.

- Measured the largest official panorama (`dataset/Панорамы/16.jpg`, `27025 x 21227`, 574 Mpx) end-to-end on zelda RTX 4090 with the default ML sulfide (SegFormer-B2) + ML talc (SegFormer-B0) CLI path: wall `7:07.85`, peak GPU memory `4777 MiB` (constant — does not grow with panorama size), peak RSS `~20.1 GiB`, exit 0. Evidence in `outputs/benchmarks/largest_panorama_16jpg_zelda_20260705/`, write-up in `docs/benchmarks/08_largest_panorama_16jpg_zelda_20260705.md`. Updated `presentation/presentation_ru.md` performance claims (slides 9/14/16 now cite benchmarks 07+08 instead of "panorama benchmark pending"), re-rendered `presentation/presentation.html`, and extended `presentation/google_slides_draft_ru.md` with the measured panorama facts.

- Reran the optimized full-resolution heuristic-only panorama benchmark on Mac and zelda after ROI component classification. Mac M2 Max completed `dataset/Панорамы/13.jpg` in `130.71s` (`4413` components), while zelda completed in `569.43s` on `/dev/shm` scratch and therefore still fails the 5-minute target for the heuristic-only path. Evidence is under `outputs/benchmarks/heuristic_roi_component_20260705_mac/` and `outputs/benchmarks/heuristic_roi_component_20260705_zelda/`.

- Recorded QA session #4 fact (`docs/notes/2026-07-03-official-metrics-and-panorama-split.md`): organizers, asked for the talc fraction of the blue-line images, confirmed they **do not have it and cannot provide it** — so the "±3% talc-fraction" criterion has **no reference for anyone** and is not gradeable. Reframed the compliance verdict in `docs/notes/2026-07-05-consolidated-metrics.md` §5 from "❌ not met" to "⛔ not gradeable (no GT exists)", keeping the honest proxies (OOF error ~5.5 pp; talc>10% decision agreement 90%; talcose F1 0.851).
- Measured the **talc-fraction error** against the organizers' "±3%" criterion via a new leak-free OOF harness `scripts/eval_talc_fraction_error.py` (each of the 42 expert blue-contour images scored by the SegFormer-B0 fold where it was in validation, at that fold's calibrated threshold), run on gx10. Result (`outputs/evaluations/talc_fraction_error_oof_20260705.*`): median abs error **~5.5 pp** (image denom), mean ~7.8 pp, only **~33% of images within ±3pp** (mean signed +2.4 pp). **Verdict: the ±3% criterion is not met and not certifiable** — the dataset has no true expert talc-fraction GT (only folder labels + 42 hand-drawn contour examples), so the error is measured against weak auto-converted masks on the talc-heavy subset. By contrast the **intergrowth-type F1 ≥ 90% criterion IS met** (grade-CNN ord↔fine 0.930/0.939). Both verdicts recorded in `docs/notes/2026-07-05-consolidated-metrics.md` §5.
- Optimized standalone heuristic sulfide component classification by running contour, compactness, solidity, footprint-closing, and replacement-ratio work inside each connected component's padded ROI instead of rebuilding and morphologically processing a full-frame boolean mask per component. Added an equivalence regression test against the old full-frame reference. Focused local tests pass, and the requested gx10 full-resolution rerun on `dataset/Панорамы/13.jpg` completed in `4m54.074s` (`4413` components), versus the previous pre-fix heuristic-only gx10 run killed after `834.9s`; evidence is under `outputs/benchmarks/heuristic_roi_component_20260705_gx10/`.

- Fixed Talc Review bottom-left zoom widget behavior so `Fit` and `Actual size` / `1:1` use stable viewer bounds and reset pan in one operation; `Fit` now centers the fitted image in the visible viewer, pan gutters live on canvas margins instead of dynamic scroll-container padding, and the zoom controls use larger guarded hit targets with explicit status feedback, so repeated clicks no longer keep shifting/resizing the image or appear to do nothing.

- Added a Talc Review `Blank White` row to the top-right `Display layers` widget. When `Background` is unchecked and `Blank White` is checked, the viewer/export canvas uses a white base behind masks and overlays.

- Changed Talc Review heuristic-only overlay/legend color from orange to distinct fuchsia `#ec4899`, so heuristic mode no longer reuses colors already used by other QA states or segmentation overlays.

- Added shareable current-sample slugs to Talc Review. Opening or selecting a sample now updates the browser URL to `/sample/<slug>` (for example `2550374-2-10x`), and direct `/sample/...` links load the same review app and select the matching sample.

- Benchmarked panorama processing performance on `dataset/Панорамы/13.jpg` (`13330 x 9489`, closest available panorama to `10000 x 10000`) across the Mac M2 Max, gx10 GB10, zelda RTX 4090, and VM102 access path. The ML sulfide + ML talc path passes the 5-minute target on measured workstations (`186.9s` Mac, `99.6s` gx10 under contention, `104.3s` zelda); the full-resolution heuristic-only path does not pass (`>300s` local timeout, `>834s` remote killed). VM102 could not be measured because the QEMU guest agent is down and no usable guest IP/SSH path was visible. Evidence and exact commands are in `docs/benchmarks/07_panorama_performance_20260705.md`.

- Full-pipeline benchmark with the requested backends — **sulfide SegFormer-B2 + talc SegFormer-B0 + heuristic (deterministic-rule) grain classification** — on the deconflicted 345 split, run on **gx10 (GB10, CUDA)** (`outputs/evaluations/bench_b2_b0_rule_gx10_20260705/`). Added `--talc-checkpoint`/`--talc-threshold` passthrough to `scripts/evaluate_official_pipeline.py`. Result: deterministic-rule **macro-F1 0.508** (row 0.207 / fine 0.464 / **talcose 0.851**), acc 0.539; feature-CV (ExtraTrees) **0.770** (talc 0.847). Headline: swapping the color auto-candidate for the trained B0 talc model lifts the rule pipeline **0.185 → 0.508**, with talcose **0.000 → 0.851** — the talc grade is effectively solved; ordinary↔fine stays the rule's weak axis (handled by the grade-CNN 0.93). Consolidated all numbers into `docs/notes/2026-07-05-consolidated-metrics.md`. Compute moved off the Mac per user direction (MPS parallel workers OOM'd; MPS sequential ~1.5-2h); gx10 ran it in minutes. zelda (RTX 4090 spot) attempted for a cross-GPU check but its WAN link could not sustain the checkpoint transfer even chunked (dataset landed, 328 MB B2 kept dropping).

- Added an editable `ML talc probability threshold` field to the Talc Review `Neural Model` panel. It shows the current default `0.50`, clamps values to `0..1`, and passes the selected threshold into per-sample `Run model`.

- Changed the Talc Review `Download` action to a compact icon-only button with the existing hover/accessibility label.

- Added a Grain Review signal for suspected fragmented hard-to-process grains: fine-intergrowth source grains whose component area is at or below the dataset p20 area threshold and whose solidity/compactness still look ordinary now get a `мелк` badge, a detail/Tinder explanation, and extra weight in `ценные для проверки` sorting.

- Added a Talc Review `Download` action next to Save/Next. It exports the current sample as a full-resolution PNG with the enabled background, editable class masks, Display layers, comparison overlay, and talc-cluster overlay, while excluding transient tool cursors and edit handles.

- Added shareable Grain Review Tinder slugs. `/tinder/<grain_uid>` now serves the app directly in `Tinder mode`, `/api/page?focus=<grain_uid>` shifts pagination to include the requested grain, and the browser URL updates as the reviewer advances through Tinder cards.

- Documented a **critical upload decompression-bomb DoS** verified against the production deployment. `Image.MAX_IMAGE_PIXELS = None` (apps/ore_pipeline_web.py:78) disables PIL's bomb guard, and the upload path decodes the full raster with no dimension check, so a 46000×46000 solid-color PNG (6.17 MB on the wire) decodes to ~6.3 GB of RAM. 24 concurrent such uploads (~148 MB total) took the 67 GB / 16-vCPU production server fully offline for ~40 s, drove free RAM to 3.7%, OOM-killed the backend (all clients got HTTP 502), and forced a supervisor restart. Findings, timeline, and prioritized fixes (restore pixel cap, reject by declared dimensions pre-decode, bound heavy-op concurrency, lower MAX_UPLOAD_BYTES + Caddy body limit, stream multipart to disk) recorded in `docs/benchmarks/06_production_load_stability_20260704.md`. No code fix applied yet.

- Added a short-lived (1s TTL) cache for the expensive `/api/status` computation in `apps/ore_pipeline_web.py`. External load testing of the production deployment (behind Caddy basic-auth) showed `/api/status` — the endpoint the UI polls continuously — capping at ~80 req/s with throughput flat versus concurrency and latency collapsing (p50 ~1s at c=100, unresponsive at c=200): the classic GIL-serialization signature, driven by 3 filesystem size walks plus disk/memory/cpu/gpu probes recomputed on every request. `OrePipelineHTTPServer.status_payload()` now serves the store computation from a cache guarded by a lock (which also collapses a burst of concurrent requests into a single recompute), while still injecting a fresh access log on every request so the live log is never stale. New regression test `test_status_payload_caches_expensive_computation_but_refreshes_access_log`. See `docs/benchmarks/06_production_load_stability_20260704.md`.

## 2026-07-04

- Added repo-root `compose.yaml` as the primary Docker deployment entrypoint. The default service keeps the lightweight heuristic UI path, while the opt-in `gpu` profile covers gx10/ML with the SOTA image, NVIDIA device reservation, model mounts, and talc-output checkpoint mount. Updated Docker tests and deployment docs; the old `docker-compose.ore-pipeline-ui.yml` remains as a compatibility file.

- Added a visible `v2` app version badge to the ore-pipeline top navigation between History and the language selector, and exposed the same value as `app.version` in `/api/status`.

- Changed the default ore-pipeline talc segmentation backend to ML when the SegFormer-B0 talc checkpoint is available. Settings reset/defaults now select `ML model` for `Бэкенд сегментации талька`.

- Clarified Settings / Runtime talc thresholds: the Settings field is now labeled as an ML talc probability threshold and is disabled while the talc backend is `heuristics`, since heuristic talc paths do not consume that probability cutoff.

- Added explicit hover/focus tooltips to the ore-pipeline viewer zoom controls in both the main view and Edit & Recalculate dialog, using the existing Russian/English labels for Fit, Actual size, Zoom in, and Zoom out.

- Moved the Talc Review `Original blue lines` overlay toggle into the top-right `Display layers` widget. It now sits with `Background`, `Talc cluster areas`, and `Sulfides`, while preserving the same raw blue-stroke overlay behavior.

- Added `Heuristic vs Neural Model` to Talc Review `Comparison mode`. It compares the non-neural talc-zone mask directly against the neural talc mask, keeps both source run actions available, and highlights agreement, heuristic-only, neural-only, and sulfide-conflict pixels.

- Added an MCP (Model Context Protocol) server, `apps/ore_mcp_server.py`, exposing the ore pipeline as tools over stdio. `classify_thin_section(image_path, out_dir=None)` wraps `ResidentSulfidePipeline.run_image` with the model held **warm** in-process (lazy load on first call, no per-image reload — this is "Option A"), returning `result_quality`, `talc_source`, the inlined `ore_summary` metrics, an optional learned grade branch, and artifact paths. `get_config()` reports resolved checkpoints/device without loading models. Checkpoints default to the same paths as the web app, overridable via `ORE_MCP_*` env vars. New dep `mcp>=1.2.0`; covered by `tests/test_ore_mcp_server.py` and a manual end-to-end run (`docs/notes/2026-07-04-mcp-server.md`).

- Added a Talc Review `Sulfides` display layer to the top-right `Display layers` widget. It tints the sample sulfide mask as an independent overlay without changing the selected background, annotation masks, or sulfide-protection behavior.

- Added an OpenAPI 3.1 description of the ore-pipeline HTTP API, served verbatim at the new unauthenticated route `GET /api/openapi.json`. The document is generated from the handler route table in `build_openapi_document()` (auth, uploads, single runs, batches, artifact downloads; 33 paths / 37 operations) so the app can declare OpenAPI support. The spec route stays open even when a UI password is configured. The document passes formal `openapi-spec-validator` validation (added to `requirements-dev.txt`). Covered by `OrePipelineOpenApiTest` (valid 3.1 doc + resolvable `$ref`s, formal spec validation that skips if the validator is absent, route-token/handler sync guard, unauthenticated HTTP round-trip).

- Changed the ore-pipeline Settings Runtime action from `Test` / `Проверить` to `Test All` / `Проверить все` and separated it visually from the grain-classification backend selector.

- Added Grain Review sorting by most valuable manual-review cases. The new `ценные для проверки` sort applies before pagination and prioritizes unlabeled/uncertain grains, mixed heuristic votes, boundary-only fine warnings, threshold-near features, and larger high-impact grains; cards and detail views show the review-value score and reasons.

- Added per-run backend selection to the Workspace `Configuration...` dialog: sulfide segmentation can use `Sulfide Heuristics` or `ML Sulfide (SegFormer-B2)`, talc segmentation can use `Talc Heuristics` or `ML Talc SegFormer-B0`, and grain classification can use `Ore Grain Heuristics` or `ML Ore Grain Classification`. Settings now provide the defaults: ML sulfide, heuristic talc, and heuristic grain. Run provenance, Status, Series settings, Runtime Test, and technical details now record/display the selected paths.

- Fixed Talc Review `Display layers` overlay positioning before and after image load. The top overlay row is hidden until it is measured, wraps inside the visible viewer width, and caps its right edge to the center work pane so the top-right widget cannot spill into the side settings panel.

- Enabled Caddy reviewer Basic Auth for the Alize v2 production endpoints. Both `https://nornickel-ai-hackathon.alola.ru/` and `http://111.88.124.80/` now require username `reviewer`; unauthenticated and wrong-password requests return `401`, while authenticated `/workspace` and `/api/status` still reach the ML backend on `NVIDIA L4`.

- Enabled plain HTTP IP access for the Alize v2 deployment. Caddy now serves `http://111.88.124.80/` to the same private backend as the HTTPS hostname; with reviewer authentication, root redirects to `/workspace`, `/workspace` returns `200`, and `/api/status` reports health `ok`, backend `ml`, and GPU `NVIDIA L4`.

- Made heuristics the default talc segmentation backend for new ore-pipeline UI settings, Settings reset, and CLI startup while keeping `ML model` selectable.

- Made the ore-pipeline Settings runtime control explicit for talc segmentation: `Talc segmentation backend` / `Бэкенд сегментации талька` now offers `heuristics` and `ML model` labels while preserving the existing `heuristic` / `ml` settings values.

- Added a Talc Review Neural Model `Run model` action for the selected sample. It runs the configured talc segmentation checkpoint through `scripts/infer_talc_segmentation.py`, writes `qa/neural_talc_model/` artifacts plus `model_talc_mask.png`, and refreshes `Neural Model` / `Current vs Neural Model` overlays without leaving the sample.

- Added `presentation/google_slides_draft_ru.md` — paste-ready slide texts for the official Google Slides template (Сложность задачи, Наш пайплайн, Сильные/Слабые стороны, Обучение сульфид- и talc-сегментации, Функционал решения, Как запустить), condensed from `presentation/presentation_ru.md`.

- Updated `presentation/features_ru.md` and regenerated `presentation/features.html`: added REST API + OpenAPI 3.1 (`GET /api/openapi.json`), the new MCP server (`apps/ore_mcp_server.py`) as an AI-agent interface, and the implemented-but-undocumented GIS export (GeoJSON/Shapefile).

- Corrected the public Alize production deployment to the v2 ore pipeline UI from `apps/ore_pipeline_web.py`. The live `https://nornickel-ai-hackathon.alola.ru/` backend now runs `nornickel-ore-pipeline-ui:v2-ml` as `nornickel-ore-pipeline-ui-v2` on private `127.0.0.1:8765->8080`, with SegFormer-B2 sulfide, SegFormer-B0 talc, and pp-aware EfficientNet-B3 grade checkpoints mounted read-only. Public `/workspace`, `/api/status`, `/api/runtime/test`, and upload/run smoke `run_20260704_180453_030668504_b9327d1a` pass. Evidence is in `docs/ui/v2/notes/2026-07-04-alize-v2-production-deploy.md`; the infra repo was updated to mark the earlier legacy `nornickel-qc-ui` deployment as superseded.

- Fixed Talc Review `Display layers` overlay anchoring during browser resize. The top overlay row now clamps to the visible viewer/viewport intersection, keeping the top-right widget inside the browser viewport instead of letting it run off-screen.

- Added explicit in-app hover/focus tooltips for the Talc Review toolbar controls so icon-only tool buttons show their descriptions immediately, including when hovering the SVG icon itself instead of relying on native browser title popups.

- Added a separate checked-by-default Grain Review crop `background` checkbox. The `контур зерна` checkbox now only toggles the grain contour overlay; unchecking `background` hides only the base crop image while keeping the viewer size and contour overlay intact.

- Changed the Talc Review `Similar` toolbar icon to an explicit magic-wand glyph with sparkles so it does not read as an eraser.

- Changed the Talc Review top toolbar drawing tools to compact icon buttons with hover titles for Brush, Fill, Similar, Rectangle, and Polygon, and removed the duplicate top-toolbar Zoom In/Zoom Out/Fit controls plus toolbar zoom percentage now covered by the bottom-left zoom widget.

- Fixed Talc Review toolbar wrapping at narrowed window widths. The toolbar and Save/Save & Next/Next actions now live in a real wrapping flex container that contributes its full height to the topbar, so second-row controls push the viewer down instead of being hidden behind it.

- Fixed Talc Review top overlay positioning after sample load. The `Segmentation classes` and top-right `Display layers` widgets are now fixed to the visible viewer viewport and refreshed after sample layout updates, so the free-pan scroll origin no longer pushes them off-screen when the image appears.

- Added an optional `контур зерна` checkbox to Grain Review crop viewers. When enabled, the app serves a transparent contour overlay from `/contours/<grain_uid>` by locating the source batch sulfide mask, selecting the connected component for the current grain, and drawing its boundary over the zoomed crop image.

- Changed the Grain Review heuristic explanation from inline semicolon-separated text to a table with feature, current value, fine-threshold, and ordinary/fine vote columns in both the detail panel and `Tinder mode`.

- Added ordinary/fine heuristic percentages to Grain Review verdicts. The detail panel and `Tinder mode` now show `Счёт эвристики: рядовое X% · тонкое Y%`, computed from the three existing fine-threshold votes without changing label persistence or keyboard behavior.

- Changed the grain review app's three-choice action order from `рядовое | тонкое | ?` to `тонкое | ? | рядовое` in both grid cards and the side detail panel, matching the Tinder-mode left/down/right decision semantics while keeping label values and keyboard shortcuts unchanged.

- Added `Tinder mode` to the grain review app (`apps/grain_review_web.py`). The app now serves source images by manifest grain UID, includes bbox/source metadata in `/api/page`, and can review one grain at a time with the original image plus stroked grain bbox, zoomed crop, feature details, and arrow-key decisions: `←` тонкое / `fine_intergrowth`, `→` рядовое / `ordinary_intergrowth`, `↑` postpone without saving, `↓` uncertain. Focused unit tests and Playwright browser smoke cover the new source endpoint, overlay bbox, and arrow flow.

- Ran a live UX smoke test of the v2 ore pipeline UI on `127.0.0.1:63589` and documented results in `docs/ui/v2/notes/2026-07-04-ore-pipeline-ui-ux-smoke.md`. Confirmed no browser console errors on the checked pages, verified the final legend percentages/separators, talc-cluster default/color, Russian mouse hints, preprocessing popup fit, Status model spacing, side-by-side splitter practical `0..1` range, and Edit & Recalculate brush/zoom controls. Remaining UX findings are layout issues: Status page fixed-width overflow, Workspace viewer-option row wrapping, Edit & Recalculate statistics label wrapping, and Workspace mobile overflow.

- Path B combined headline: ran the grain-grade aggregation with **both** levers — trained B0 talc model (`--talc-checkpoint`) **and** the variant-A replacement gate (`--fine-dark-inside-floor 0.08`) — on the 345 split (bootstrap grain labels, leak-free grouped CV). **Grade macro-F1 0.612** (row_ore 0.386 / fine 0.609 / talcose 0.841), accuracy 0.638; the two levers are additive (progression 0.190 → 0.513 talc → 0.612 talc+A). Remaining gap is the ordinary↔fine axis (row recall 0.28) → human grain labels. Artifact `outputs/evaluations/grade_from_grains_talc_floor008_v0/`; recorded in `docs/plans/39`. Also fixed one canonical name — our grade classifier is **"Grade-CNN (path A)"** across jury-facing docs (presentation + notes).
- Ran a **4-class grade benchmark** (ordinary/thin/talc/refractory — an alternate schema) via a new `train_grade_classifier.py --four-class` mode: 4-class labels from folders (fine → thin/refractory by `/тонкие/`), 4-class-aware sha256 dedup + conflict drop (70 conflict + 21 dup paths), grouped-by-аншлиф train/val (train 926 / val 163), class-weighted CE, plain augs. Result (efficientnet_b3 @384, gx10): **val F1-macro 0.771** (ordinary 0.906 / thin 0.792 / talc 0.865 / refractory 0.522); the weak class is refractory (only 56 refractory images total → noisy). Artifacts `models/grade_classifier/effb3_4class_20260704/`. The deployed grade branch stays 2-class (ordinary↔fine 0.93); this is a schema-variant benchmark, not the production head.

- Added a blank line between the binary sulfide and talc details in the Status page `Models` card, so long checkpoint paths no longer read as one dense paragraph.

- Changed the Workspace `Configuration...` save behavior so changed run configuration values immediately invalidate derived state. The UI now clears any current completed/prepared run, generated augmented/preprocessed display layers, sulfide/final layers, metrics, exports, grain outlines, run-file previews, and tiling while preserving the original upload, metadata, checkbox/settings values, and upload-level artefact masks; pressing Start creates a fresh immutable run with the new configuration.

- Added `presentation/videos/METADATA.md` with upload-ready Russian titles, descriptions with timecodes, `Наука и технологии` category, and `Для всех` access metadata for each final presentation video, including the gx10 Silero Talc UI variant.

- Restyled the Talc Review bottom-left zoom control to match the compact v2 vertical widget: Fit, Actual size, Zoom in, live percentage, and Zoom out are stacked in one column with `32 px` icon buttons, `18 px` icons, and an `11 px` percentage label.

- Moved Workspace Edit & Recalculate zoom controls out of the top toolbar into the same bottom-left Fit / Actual size / Zoom in / live percent / Zoom out widget used by the main viewer, and added the same mouse-wheel zoom / wheel-press pan hint row below the editor canvas.

- Added Talc Review pan gutters around the canvas. Fit/Actual now start at a neutral gutter origin, and mouse-wheel / middle-button panning can move the canvas past the viewer edges so the image top-left can become negative inside the visible viewport, matching the v2 ore UI free-pan behavior.

- Changed the Workspace Edit & Recalculate Brush size control from a small numeric field to the Talc Annotation-style `2-240 px` range slider with a live `px` value and matching `28 px` default.

- Changed the Workspace `кластеры талька` / `talc cluster areas` color from pink to cyan/light blue in both the final segmentation legend swatch and the generated talc-cluster overlay. Completed runs with old display overlays are refreshed on load when their display manifest has a stale talc-cluster RGB marker.

- Added and benchmarked Mask2Former-Swin-Tiny support for binary sulfide segmentation. The training/eval path now accepts `--model mask2former`, projects Mask2Former query outputs into dense binary logits for shared inference/evaluation, and has focused `model_io` coverage. Zelda `root@111.88.124.23` completed a 30-epoch run on `binary_sulfide_dataset_v0`: best sulfide IoU `0.968313`, F1 `0.983901`, AUC `0.998492`, HD95 mean `29.55 px`, average `226.39 s/epoch`. SegFormer-B2 remains the default because it is better on IoU/F1/AUC/HD95 and faster. Artifacts are mirrored under `models/binary_sulfide/mask2former_swin_tiny_dataset_v0_zelda_20260704_1553/`; extended metrics are in `outputs/evaluations/mask2former_best_eval_metrics.json`; benchmark docs updated.

- Added and benchmarked SegFormer-B3 support for binary sulfide segmentation. The training/eval path now accepts and loads `segformer_b3` with the `nvidia/mit-b3` shape, plus focused `model_io` regression coverage. Zelda `root@111.88.124.23` completed a 30-epoch run on `binary_sulfide_dataset_v0`: best epoch 26, sulfide IoU `0.973350`, F1 `0.986495`, AUC `0.998784`, HD95 mean `25.20 px`, average `141.41 s/epoch`. B3 is larger/slower than B2 and below B2 on IoU/F1/AUC/HD95, so SegFormer-B2 remains the default. Artifacts are mirrored under `models/binary_sulfide/segformer_b3_dataset_v0_zelda_20260704_1814/`; extended metrics are in `outputs/evaluations/segformer_b3_best_eval_metrics.json`; benchmark/model-card/status docs updated.

- Changed the Workspace final segmentation legend so `кластеры талька` / `talc cluster areas` is unchecked by default. The layer remains available in both left and side-by-side right legends, and resets return it to off.

- Removed horizontal overflow from the Workspace `Настройки предобработки` popup. The dialog now uses border-box sizing, wraps its footer hint, and stacks panorama scaling fields at narrow widths instead of showing a horizontal scrollbar.

- Added separator lines to the Workspace final segmentation legend overlays. Both left and side-by-side right legends now group `ordinary/fine/talc`, then `talc clusters/artefacts`, then `background`, while keeping per-class percentage rows.

- Enabled Talc Review wheel-press panning from the actual mouse-wheel / middle-button press. The canvas now starts pan before edit-tool handling, suppresses native middle-click browser behavior, and keeps a mouse-event fallback for platforms that do not deliver middle-button pointer events.

- Localized the Workspace mouse-navigation hints in Russian mode: `Колесо мыши - масштаб` and `Нажатие колеса - панорама`, while keeping the English strings for English UI mode.

- Moved Talc Review `Background` and `Talc cluster areas` out of the Segmentation classes widget into a separate top-right `Display layers` widget. The left widget now contains only editable classes and target status, while the right widget owns display-only layer controls and the cluster-area percentage.

- Added a `Contour width` / `толщина контура` slider next to the Workspace `contours only` viewer option. The slider controls contour thickness for sulfide/final overlays and shared comparison exports while preserving the previous `1 px` default.

- Added the v2 ore UI mouse-navigation hint row below the Talc Review main viewer: `Mouse wheel - zoom in / out` and `Mouse wheel press - pan`.

- Updated the Workspace bottom-left zoom widget layout so `Fit view` is stacked directly above `Actual size / 1:1`, followed by zoom in, live percentage, and zoom out.

- Added a bottom-left zoom widget to the Talc Review viewer, matching the v2 ore UI control pattern: Fit, Actual size, Zoom in, live zoom percentage, and Zoom out. The widget shares the same zoom state as the toolbar buttons and mouse-wheel zoom.

- Fixed the Workspace side-by-side splitter drag range. The comparison divider now clamps to the full `0..1` view range, so it can be moved to the exact left or right edge instead of stopping around 12%-88%.

- Added client-side sorting to the Workspace `Сульфидные зерна` / sulfide-grain table. Data headers now sort rows by ID, type, area, equivalent diameter, perimeter, sulfide share, liberation, contacts, or locked/composite proxy, with a second click reversing direction and the total row fixed at the bottom.

- Updated the Talc Review `Comparison mode` selector to the requested five labels: `Current`, `Heuristic`, `Neural Model`, `Current vs Heuristic`, and `Current vs Neural Model`. Plain heuristic/neural modes show only their prediction layer, while the `Current vs ...` modes keep the agreement/current-only/prediction-only comparison overlays.

- Added a separated `Background` visibility checkbox to the Talc Review segmentation-class widget. It is checked by default, hides only the base image when unchecked, keeps selected mask/comparison/cluster overlays visible, suppresses missing-background warnings while off, and records `background_visible` in review view settings.

- Added optional Settings-managed password protection to the v2 ore pipeline UI. Settings now has a Security panel with a write-only password field and explicit remove-password checkbox; saved passwords are stored only as salted PBKDF2-SHA256 hashes, `/api/settings` exposes only `auth.password_enabled`, `/login` issues an HttpOnly session cookie, and protected UI/API/artifact requests redirect or return `401` once enabled. Added spec/plan docs and focused HTTP/static regression tests.

- Added Workspace viewer navigation affordances: the viewer-options row now shows mouse icons with `Mouse wheel - zoom in / out` and `Mouse wheel press - pan` hints, and the canvas has a bottom-left zoom widget with fit-view, actual-size, zoom-in, current zoom, and zoom-out controls. Focused ore-pipeline web tests pass, and the local service was verified on `127.0.0.1:63589/workspace`.

- Cherry-picked the standalone non-neural talcose/not-talcose classifier CLI into v2 and wired it into the Talc Review browser comparison modes. The CLI now lives in `scripts/classify_talcose_heuristic.py` with reusable core logic in `src/ore_classifier/talc_zone_heuristic.py`; the web app calls `/api/samples/{sample_id}/talcose-heuristic`, uses the sample sulfide mask as the ore-exclusion mask when available, writes `qa/non_neural_talcose/{talc_zone_mask.png,talc_flake_mask.png,overlay.jpg,talcose_result.json}`, exposes those artifacts in `sample_payload()`, and paints heuristic/neural standalone or current-comparison overlays on the canvas. Focused non-neural CLI, Talc Review web, and neural talc segmentation smoke tests pass.

- Deprecated the legacy Streamlit QA tools: moved `apps/talc_review_streamlit.py`, `apps/sulfide_qa_streamlit.py`, and the `apps/components/mask_shape_editor` custom component into `apps/deprecated/streamlit/`. Fixed the moved talc app's `PROJECT_ROOT`/component paths for the new depth, and updated all run-command references in `README.md`, `apps/README.md`, `COMMANDS.md`, `SMOKE_TESTS.md`, `scripts/prepare_manual_review_pack.py`, and the talc UI docs. The active tools remain the plain web apps (`apps/talc_review_web.py`, `apps/ore_pipeline_web.py`, `apps/grain_review_web.py`).

- Added QA#3 as an official-source artifact: downloaded `https://disk.360.yandex.ru/i/lSyyoiK7LfeahQ`, generated Russian Whisper `small` CPU STT at `docs/official/2026-07-04-qna-session-3-transcript.md`, and updated `docs/official/QnA_sessions.md` with exact QA#3 source video/timestamps. Key clarified deltas: `тонкие = труднообогатимые`, talc is preferably semantic but pixel counting is acceptable, phase inventory is not task-critical unless justified, and the ideal prototype colors/analyzes whole panoramas while fragment classification is the minimum realistic path.

- Extended ore-pipeline file serving to support bounded-memory byte-range streaming. `send_file()` now advertises `Accept-Ranges: bytes`, returns `206 Partial Content` with range-sized `Content-Length` / `Content-Range` for valid single ranges, and returns `416` with `Content-Range: bytes */<size>` for unsatisfiable ranges. The existing full artifact/report/ZIP downloads still stream in chunks without `Path.read_bytes()`. Focused and full `tests/test_ore_pipeline_web.py` pass.

- Renamed the `/history` run table Progress column to Status. Active rows now show only a text percent, failed/error rows show `Error` / `Ошибка` with a hoverable `(i)` detail marker backed by the stored run error, and completed rows show `Done` / `Готово`. Focused static UI coverage and the served `/history` page were verified.

- Excluded heavy audio/video artifacts from git: added `*.wav` / `*.mp3` / `*.mp4` to `.gitignore` and untracked all 121 such files under `presentation/videos/` (amended into the unpushed batch commit, so ~330 MB of media never enters remote history). The files stay on disk; regenerate via `scripts/generate_talc_ui_video*.sh` or per-bundle `build_video.py`, checksums remain in `presentation/videos/manifest.sha256`.

- Moved the generated demo-video bundles from ignored `outputs/demo_video_*` directories into the presentation package under `presentation/videos/`, regenerated all three 5-minute MP4s in place, refreshed Whisper STT text under `stt_refresh_20260704/`, and kept `outputs/demo_video_*` symlinks only for compatibility. Added a local video index, STT refresh summary, and checksum manifest in `presentation/videos/`.

- Deployed a dedicated gx10-local TTS/video-generation toolchain for the Talc UI demo under `ashmelev@192.168.86.14:/home/ashmelev/Projects/nornikel_tts_video`, separate from the live ore-pipeline web UI on `:8210`. `presentation/videos/demo_video_talc_ui_20260704/build_video.py` now supports `TTS_BACKEND=silero` with the Russian male Silero `aidar` speaker, Linux font fallback, and user-space ffmpeg via `imageio-ffmpeg`; `scripts/generate_talc_ui_video_gx10.sh` pushes the generator/scenes to gx10, runs local TTS/video generation, validates the 300 s MP4/subtitles, and can pull the result back. Verified gx10 output: `nornikel_talc_ui_demo_1080p_ru.mp4`, 300.0 s, 1920x1080 H.264, AAC 48 kHz stereo, Russian-only subtitles. Cached higher-end TTS assets in gx10 shared model caches: `coqui/XTTS-v2` (2.1 GB) and `hotstone228/F5-TTS-Russian` (4.7 GB). Deployment notes are in `docs/ui/v2/notes/2026-07-04-gx10-tts-video-deployment.md`.

- Ran an independent Codex isolated gx10 rerun of the ch1/ch2 class-folder E2E batch without touching the earlier Claude output directory. The isolated workspace was `gx10:~/Projects/nornikel_codex_e2e_eval_20260704_1333/`, with top-level evidence copied locally to `outputs/evaluations/codex_ch1_ch2_class_folder_eval_20260704_1333/` and exact steps documented in `docs/benchmarks/04_ch1_ch2_class_folder_codex_gx10_e2e_eval.md`. Split: `1180` source paths, `48` cross-label duplicate paths skipped, `1132` selected (`471` fine, `543` ordinary, `118` talcose). Inference: resident B2 sulfide + B0 talc on CUDA, `0` failures, `2026-07-04T13:37:43+03:00` to `14:46:50+03:00`. Metrics: deterministic rule accuracy `0.4293`, macro-F1 `0.4642`, weighted-F1 `0.3992`, macro AUC `0.6266`; feature-CV best `random_forest` accuracy `0.7800`, macro-F1 `0.7699`, weighted-F1 `0.7771`, macro AUC `0.9025`; fixed talc gate TP/FP/FN/TN `109/116/9/898`, precision `0.4844`, recall `0.9237`, F1 `0.6356`, binary accuracy `0.8896`.

- Fixed ore-pipeline talc cluster areas so they cannot overlap sulfide regions. Cluster generation now accepts the sulfide mask as an exclusion mask, removes those pixels from the talc-cluster density analysis, persists non-overlapping `masks/talc_cluster_mask.png`, and repairs older overlapping cluster masks when display layers are rebuilt. Added focused regression coverage and verified `tests/test_ore_pipeline_web.py` passes.

- Analyzed the official presentation template `presentation/template/Норникель 2026.pptx` (Google Slides export: 9 slides, 4 layouts, image-based layout backgrounds, no real placeholders, Proxima Nova + brand colors `#5302E0`/`#00FFBF`/`#434343`) and wrote the structure findings to `presentation/template/template-structure.md`. Added project repository links (GitHub primary, SourceCraft auto-sync mirror) to the appendix sources slide of `presentation/presentation_ru.md`.

- Added a `Repositories` section to `README.md`: the main repo is on GitHub (`https://github.com/s1mb1o/2026-ore-thin-section-classifier`), and `https://sourcecraft.dev/s1mb1o/2026-ore-thin-section-classifier` is an auto-sync mirror.

- Clipped talc review `Talc cluster areas` to non-sulfide pixels. The browser cluster overlay now ignores sulfide-mask pixels when building local talc density, never paints cluster highlight over sulfides, and records `sulfide_excluded_pixels` / `non_sulfide_pixels` in the cluster view metadata. Updated focused UI contract tests and talc-review docs.

- Added Model-vs-Human QA and hard-negative annotation support to the talc browser review UI. `apps/talc_review_web.py` now supports a third editable `Not Talc` class (`current_not_talc_mask.png` / `reviewed_not_talc_mask.png`) that is excluded from `talc_node`, overlays optional trained model talc masks against the current human Talc mask with agreement/model-only/human-only/sulfide-conflict colors, supports teammate human masks for agreement/disagreement review, and upgrades `Similar` from one intensity seed to positive/negative luma/color/texture seeds that also reuse `Not Talc` as negative examples. Added spec/plan docs and focused tests for hard-negative save semantics, model/teammate mask discovery, and the new browser controls.

- Upgraded the live gx10 v2 ore-pipeline UI deployment at `http://192.168.86.14:8210/workspace` from the lightweight heuristic Docker image to a CUDA-capable SOTA ML image `nornikel/ore-pipeline-ui:v2-gx10-ml` based on `nvcr.io/nvidia/pytorch:25.11-py3`. The container `nornikel-ore-pipeline-ui-v2` now runs with SegFormer-B2 binary sulfide, SegFormer-B0 talc, talc threshold `0.50`, and the pp-aware EfficientNet-B3 grade checkpoint mounted read-only. Added entrypoint/Compose support for talc and grade checkpoint env vars, added the gx10 ML Dockerfile plus focused Docker test coverage, updated the Docker runtime spec/runbook, and saved deployment evidence in `docs/ui/v2/notes/2026-07-04-ore-pipeline-docker-gx10-sota-ml-deploy.md`. Verification: Docker artifact test passed, `py_compile` passed, image import smoke saw CUDA on `NVIDIA GB10`, `GET /workspace` returned `200`, `POST /api/runtime/test` loaded B2+B0 on CUDA (`ok=true`), and a real ML upload/run on `DSCN2176.JPG` completed in `17.143` s with the grade branch present.

- Added `scripts/generate_talc_ui_video.sh`, a repo-level wrapper for regenerating the dedicated Talc UI demo video from `presentation/videos/demo_video_talc_ui_20260704/build_video.py` and captured scene screenshots. It selects the existing TTS Python venv when present, validates the final MP4 with ffprobe (`300.000000` s, H.264, 1920x1080, AAC), rebuilds `contact_sheet.jpg`, and can optionally rerun Whisper STT. Documented the command and `CHECK_ONLY=1` validation mode in `COMMANDS.md`.

- Created a dedicated Talc UI tool demo video artifact under `presentation/videos/demo_video_talc_ui_20260704/`: `nornikel_talc_ui_demo_1080p_ru.mp4` is a 5:00 1920x1080 MP4 based on `docs/ui/v2/notes/2026-07-04-talc-ui-video-script-ru.md`, with Russian male TTS (`ru-RU-DmitryNeural`), burned-in Russian subtitles, corner time marks, and only the Talc mask review UI/screens. The video covers original/MS Paint/QA backgrounds, `Positive bag` vs `Talc`, cluster areas, Brush/Fill/Rectangle/Polygon, Similar/SAM2/Dark pixel preview, sulfide protection, and reviewed mask outputs. Saved the timed script, SRT, timeline/build metadata, screenshot/contact sheets, Whisper STT outputs, and `stt_verification_report.md`; ffprobe confirms 300.000 s video/audio, and STT/visual verification passes with expected phonetic STT drift on English UI labels.

- Added a Workspace viewer `Share comparison` / `Поделиться сравнением` action next to `Fix me`. When a right-side comparison layer is selected, it exports a client-side PNG that places the current left layer and right comparison layer at full display-preview resolution in one row, preserving overlay class visibility, opacity, contour mode, and selected sulfide-grain outlines. The service was restarted on `127.0.0.1:63589`; static UI and web tests pass.

- Created a dedicated v2-UI-only demo video artifact under `presentation/videos/demo_video_v2_ui_only_20260704/`: `nornikel_v2_ui_only_demo_1080p_ru.mp4` is a 5:00 1920x1080 MP4 with Russian male TTS (`ru-RU-DmitryNeural`), burned-in Russian subtitles, corner time marks, and only the main ore-pipeline v2 UI screens (Workspace, History, Series, Settings, Status, API, run layers/metrics/files, panorama/tiling). Saved the timed script (`script_ru.md`), SRT (`subtitles_ru.srt`), timeline/build metadata, visual contact sheet, Whisper STT outputs, and `stt_verification_report.md`; ffprobe confirms 300.000 s video/audio, and STT/visual verification confirms the narration matches the UI-only script after phonetic adjudication of technical English terms.

- Created the v2 UI demo video artifact under `presentation/videos/demo_video_v2_ui_20260704/`: `nornikel_v2_ui_demo_1080p_ru.mp4` is a 5:00 1920x1080 MP4 with Russian male TTS (`ru-RU-DmitryNeural`), burned-in Russian subtitles, corner time marks, and real v2 UI scenes from pre-generated immutable runs plus the talc-review UI. Saved the timed script (`script_ru.md`), SRT (`subtitles_ru.srt`), build metadata, visual contact sheet, Whisper STT outputs, and `stt_verification_report.md`; ffprobe confirms 300.000 s video/audio, and STT/visual verification confirms all key spoken claims after phonetic adjudication of technical English terms.

- Ported talc cluster areas from the talc annotation tool into the v2 ore pipeline UI (spec `docs/ui/v2/specs/ore-pipeline-talc-cluster-areas-v0.1.md`, plan `docs/ui/v2/plans/43_ore-pipeline-talc-cluster-areas.md`). Settings now persist default radius/min-local-talc/opacity; each upload/run has a `Configuration...` dialog under metadata that stores effective `talc_clusterization` in immutable run metadata; final-mode viewer legends expose `talc cluster areas` in both left/right overlays with percentages; completed runs save `masks/talc_cluster_mask.png`, a `display.talc_cluster_overlay` pyramid, and summary/metric rows for talc area plus cluster area. Focused backend/static UI tests passed.

- Decision-gate analysis (`scripts/analyze_talc_grade_signal.py`, note `docs/notes/2026-07-04-talc-grade-signal.md`): tested whether the **trained** talc SegFormer-B0 gives a signal for the talcose *grade* before investing in a talcose branch. On 30 non-leak talcose vs 60 ordinary/fine (42-annotated leak-set excluded), predicted `talc_fraction_analyzed` separates talcose cleanly — median 0.685 (talcose) vs 0.019/0.014 (ordinary/fine), **talcose-vs-rest ROC-AUC 0.994**, best one-vs-rest F1 0.968 at τ≈0.25. This **reverses** the earlier "talc can't identify the оталькованная grade" conclusion: that was measured on the *color-heuristic* `talc_candidate` the pipeline feeds into the rule (≈0 on talcose), NOT on the trained segmenter. Fix = wire the trained talc segmenter (not the color candidate) into the talcose decision. Clear path to a 3-class verdict (talc-seg gate ⊕ ordinary/fine CNN); full-345 leak-aware fusion + honest τ calibration is the next step.

- Localized the official dataset in the v2 workspace. Copied the verified dataset from the legacy dataset storage into `tmp_dataset`, validated the copied manifest (`complete`, `1236/1236`, `3,018,194,503` verified bytes, `0` errors), removed the old `dataset` symlink, renamed `tmp_dataset` to `dataset`, and updated structural docs/smoke checks to expect a real local dataset directory.

- Updated the v2 ore pipeline Run files popup: image, CSV, and JSON artifacts now open in a second in-app preview dialog, while unsupported artifact types show a row-level Download action. Focused UI contract tests and user/smoke docs were updated.

- Removed the remaining non-dataset dependency on the legacy broad workspace. Copied the LumenStone `S1_v1` and `S2_v2` extracted roots into v2-local `data/external/lumenstone/full/`, added the LumenStone downloader helper, changed `build_binary_sulfide_dataset.py` defaults to those v2-local paths, ignored `data/external/` in Git/Docker, rewrote setup docs so non-dataset paths no longer point at the legacy checkout, and normalized ignored generated-output metadata from old absolute paths to the v2-local `dataset/` and `data/external/` locations.
- Added `run_grain_app.sh` (repo-root launcher for `apps/grain_review_web.py`, mirrors `run_talc_app.sh`: prefers `.venv`, defaults to `outputs/grain_dataset_v0` on an OS-assigned port, `GRAIN_HOST/PORT/DATASET_DIR` overrides, passes args through) and documented it in `COMMANDS.md`. Smoke-tested (prints URL + label count).
- Wired the **trained talc segmentation model into the path-B talcose branch** (`scripts/aggregate_grade_from_grains.py --talc-checkpoint/--talc-threshold`). A lazily-torch-imported `TalcModelScorer` loads the talc checkpoint once and computes per-image `talc_fraction = talc∩non-sulfide / analyzed` with the resident-pipeline tiling, replacing the near-zero colour auto-candidate; `τ_talc` is still calibrated per fold; per-image `talc_source` recorded (schema → v0.2). Without the flag the auto-candidate path is unchanged. **Result** (B0 talc fold_00, threshold 0.5, bootstrap grain labels, leak-free grouped CV over 345 → `outputs/evaluations/grade_from_grains_talcmodel_v0/`): grade macro-F1 **0.513** (up from v0.1 0.19), with **talcose F1 0.000 → 0.821** (P 0.76 / R 0.90) — talcose effectively solved; the remaining loss is the ordinary↔fine axis (row_ore recall 0.09, bootstrap grain classifier over-calls "fine"), which real human grain labels are the lever for. The local 3-epoch ResUNet talc checkpoint is not discriminative; the B0 full-run model is (loads via `load_binary_segmentation_checkpoint`). Tests `tests/test_grain_pipeline.py` 19/19 (+2 talc-source/grade-rule). Docs: `COMMANDS.md`, aggregator docstring, `docs/plans/39_grain-human-in-the-loop-classifier.md`, comparison note.
- Integrated the grade-classifier CNN as a **visible parallel branch** in the pipeline and web app. New reusable module `src/ore_classifier/grade_classifier.py` (`load_grade_model`/`predict_grade`, efficientnet_b3, ordinary↔fine, maps to ore-class vocabulary). `scripts/run_ore_pipeline.py` gained `--grade-checkpoint`: when set, it runs the CNN on the image and adds a `grade_branch` block (predicted label/ore-class/RU/confidence/probabilities + `grade_branch.json`) to `pipeline_summary.json` — validated end-to-end (fine image → труднообогатимая, conf 0.9999). `apps/ore_pipeline_web.py` gained a server-level `--grade-checkpoint` (defaults to the pp-aware checkpoint `models/grade_classifier/effb3_ordfine_ppaug_20260704/best.pt` when present): the model is lazy-loaded once and, in `_finalize_run_metadata`, each run gets a `grade_branch` in its summary/`run.json` plus an appended sentence in the human-readable `text_output` ("Классификатор по зерну (CNN, обычное/тонкое): … (уверенность …%)"). The branch is best-effort (a failure never fails a run) and fully inert when no checkpoint is set, so the existing suite is unaffected — `tests/test_ore_pipeline_web.py` 40/40 pass; web glue validated directly.
- Added acquisition/surface augmentation to grade training (`--augment-aug-prob`/`--augment-json`, `RandomTrainAug` applying the shared `apply_augmentation` with a per-sample random seed) to complement preprocessing-aware training (workstream to push the remaining robustness profiles); trained an aggressive variant `effb3_ordfine_ppaug07_acq_20260704` (preprocess p=0.7 + acquisition p=0.5) on gx10. Robustness sweep (`outputs/evaluations/grade_robustness_ppaug07_acq_20260704/`): it is the **worst-case-optimal** checkpoint — the previously-weakest acquisition-artifacts profile jumped 0.908→0.944 and the across-profile min improved to 0.913 (vs 0.908 for p=0.5), at a small −0.03 cost on blur/color; baseline unchanged (0.939), mean tied (0.925). p=0.5 stays the balanced web default; pp0.7+acq is the max-robustness alternative. Details in `docs/notes/2026-07-04-grade-cnn-robustness.md`.

- Fixed the grade-CNN preprocessing train/serve mismatch via **preprocessing-aware training** (spec `docs/specs/preprocessing-aware-grade-training.md`, plan `docs/plans/40`). Added `--preprocess-aug-prob`/`--preprocess-json` to `scripts/train_grade_classifier.py`: a `RandomPreprocess` transform applies the shared `ore_classifier.preprocessing.apply_preprocessing` (same as the UI) to each 384² training crop with probability p (default 0.0 = unchanged), inserted after `RandomResizedCrop` so denoise stays cheap. Retrained on gx10 with p=0.5 → `models/grade_classifier/effb3_ordfine_ppaug_20260704/` (internal-val best 0.9636). Re-ran the robustness sweep (`outputs/evaluations/grade_robustness_ppaug_20260704/`): **strict win** vs the raw-trained model — preprocess profile 0.8688→0.9174 (gap to baseline −0.062→−0.022), raw baseline 0.9303→0.9391 (no regression), and every profile improved (blur+noise 0.909→0.944, color 0.900→0.917, acquisition 0.900→0.908). Preprocessing-aware is now the preferred grade-branch checkpoint. Note updated: `docs/notes/2026-07-04-grade-cnn-robustness.md`.

- Reviewed test coverage against implemented features and filled the gaps (67% → 75% over `src/ore_classifier` + `apps`). New test files: `tests/test_resident_pipeline.py` (14 tests; resident batch inference was **0%**, now **99%** — helpers `_tile_weight`/`_as_bool_mask`/`_batched`/`_preprocess_tile`/`_save_overlay` plus real end-to-end `run_image` on a tiny CPU ResUNet checkpoint for both talc paths: `auto_candidate` and `ml_model`, asserting the talc mask never overlaps sulfide); `tests/test_grain_review_web.py` (19 tests; grain HITL app 32% → **90%** — store paging/filters/annotate persistence + full HTTP surface: page HTML, `/api/page` clamping, `/crops/` serving + traversal rejection, `/api/annotate` round-trip and error paths); `tests/test_evaluate_official_pipeline.py` (13 tests for the eval-harness's own logic: `load_settings` file/inline/invalid, `select_items` label/per-label/max-total, `build_transformed_dataset` perturbs split images preserving relative paths with lossless PNG-in-original-suffix, `build_combined_summary` shaping incl. missing-class `None`s, `render_summary_md` baseline/perturbed/n-a rendering); `tests/test_models.py` (6 tests; 28% → **98%** — ResUNet/ResidualBlock forward shapes, non-square input, gradient flow to every parameter, base-channels scaling); `tests/test_tracking.py` (14 tests; 38% → **86%** — `_scalarize` truncation, `_NullTracker` no-op contract, `add_mlflow_args` defaults, `mlflow_run` off/missing-mlflow fallbacks, `_MlflowTracker` param scalarization + numeric-only metrics + exists-guarded artifacts). Extended `tests/test_model_io.py` (3 → 12 tests; 48% → **83%** — `resolve_device` explicit/auto, `forward_logits` raw-tensor interpolation + `.logits` unwrap, ResUNet checkpoint save→load round-trip with exact-weight equality, unsupported-arch `ValueError`, actionable incompatible-checkpoint error naming path/first-key/original-error). Added `tests/browser/test_grain_review_ui_browser.py` (4 Playwright/Chromium tests driving the real grain-review DOM: grid + crop-image load, click-labeling persists to `annotations.json` and toggles off, `F`/`U` keyboard labeling with auto-advance, labeled/unlabeled view filter — all console-error-clean). Deliberately not covered: `apps/sulfide_qa_streamlit.py` / `apps/talc_review_streamlit.py` (legacy interactive Streamlit fallbacks) and the SAM2-weights-dependent part of `sam2_region_assist.py`. Full suite: **256 passed** (unit + browser).

- Grain labeling app (`apps/grain_review_web.py`) now shows the full per-grain **feature report** to support annotation decisions (mirrors the v2 pipeline's grain report). `_item_payload` surfaces all 9 morphology features plus `fine_signals`/`fine_reasons` — which of `dark_inside_ratio ≥ 0.18` / `solidity ≤ 0.62` / `compactness ≤ 0.12` tripped (thresholds mirror `ComponentRuleConfig` defaults). The UI adds a per-card `d/s/c` metric strip (triggering metrics highlighted red) and a sticky side panel with a bigger crop, the full feature table, and the heuristic verdict + reasons, so the annotator sees *why* a grain reads ordinary vs fine. Added 3 regression tests (`tests/test_grain_pipeline.py`, now 17); a consistency check confirms the app's display thresholds match the manifest's heuristic labels. Spec updated (`docs/specs/grain-human-in-the-loop-classifier.md`).
- Added `Talc cluster areas` as a separated display-only row in the talc review Segmentation classes overlay widget. Its checkbox mirrors the right-panel cluster overlay toggle, and its percentage shows highlighted cluster pixels over image pixels while preserving cluster areas as non-saved display evidence. Updated focused DOM/browser coverage and docs.
- Dark pixel preview in the talc review app now reports live visible-pixel share/count (`luma <= threshold`) for the active photo background, marks the readout inactive for mask-only/sulfide backgrounds, and records `brightness_visible_*` metadata in review patches. Added focused render/browser coverage plus docs/smoke checklist updates.
- Updated the talc review overlay widget to show live visible-pixel percentages for `Positive bag` and confirmed `Talc`, and to display the known talcose-source target `Talc >= 10% visible pixels`. The threshold status uses confirmed `talc_node` pixels rather than rough `positive_bag` pixels and flags how many percentage points are missing when the current Talc segmentation is under target. Added focused DOM contract and browser smoke assertions plus docs.
- Added a tunable display-only talc cluster overlay to `apps/talc_review_web.py`. The right panel now has `Show talc cluster areas` with source (`Talc class` or `Positive bag + Talc`), local radius, minimum local talc density, and opacity controls; the canvas highlights locally dense talc areas in magenta using a summed-area-table density map, caches the overlay between redraws, and defers recomputation during active brush/shape drags. The overlay never changes saved masks, but its settings/stats are recorded in `view_settings` for autosave/review patches. Updated the talc-review spec and focused page-contract tests.
- Validated the requested ch1/ch2 class-folder talc gate on gx10 with the current resident B2 sulfide + B0 talc pipeline. Added `scripts/build_class_folder_eval_split.py` to scan `dataset/Фото руд по сортам. ч1` and `ч2`, skip SHA-256 images that appear under multiple class labels, and write the full split/audit CSVs. Wired `--talc-checkpoint/--talc-threshold` through `scripts/run_official_batch.py`, `scripts/run_resident_batch.py`, and `src/ore_classifier/resident_pipeline.py`, so resident batch inference now produces trained talc-model outputs/provenance without per-image checkpoint reloads. Full run: `1132` deconflicted labelled images, `0` failures, overall rule macro-F1 `0.4642`; the fixed first gate `talc_fraction > 10%` gives TP/FP/FN/TN `109/116/9/898`, precision `0.4844`, recall `0.9237`, F1 `0.6356`, binary accuracy `0.8896`. Artifacts are under `outputs/evaluations/ch1_ch2_class_folder_talc_gate_20260704/`; benchmark note: `docs/benchmarks/03_ch1_ch2_class_folder_talc_gate_validation.md`.
- Robustness of the grade-CNN branch: added `--augmentation-json`/`--preprocess-json` to `scripts/evaluate_grade_branch.py` (same `ore_classifier.augmentation`/`preprocessing` transforms as the UI + seg harness; perturbs each held-out image before the CNN eval transform) and swept baseline + 4 profiles on the 230 held-out ordinary/fine (all on MPS for apples-to-apples deltas). Results (`docs/notes/2026-07-04-grade-cnn-robustness.md`, artifacts `outputs/evaluations/grade_robustness_20260704/`): baseline macro-F1 0.9303; blur+noise 0.9087 (−0.022); color shift 0.9000 (−0.030); acquisition artifacts scratches/haze/pits 0.8996 (−0.031); **UI preprocessing (illumination+denoise+contrast) 0.8688 (−0.062)**. The branch is robust to acquisition noise (worst acquisition case ≈0.90, above feature-CV 0.75 and A's 0.90/0.91); the **largest sensitivity is our own preprocessing** — a train/serve mismatch (CNN trained on raw images), fixable by serving the grade branch on raw input or adding preprocessing to train-time augmentation.
- Added **optional MLflow experiment tracking** (debug only, off by default) to the four training scripts. New shared helper `src/ore_classifier/tracking.py` exposes `add_mlflow_args(parser, default_experiment=...)` and a `mlflow_run(args, params=...)` context manager that logs params, per-epoch metrics, and final metrics/artifacts. Wired into `scripts/train_grade_classifier.py`, `train_binary_sulfide.py`, `train_talc_segmentation.py`, and `train_grain_classifier.py` behind a `--mlflow` flag (plus `--mlflow-experiment/--mlflow-tracking-uri/--mlflow-run-name`). When the flag is absent or mlflow is not installed, every tracker call is a no-op and the existing JSON/CSV/checkpoint outputs under `--out-dir` are unchanged. Default store is a local `./mlruns` file store (gitignored; browse with `mlflow ui`); the helper sets `MLFLOW_ALLOW_FILE_STORE=true` so the file store still works on mlflow>=3 (file store is in maintenance mode there). `mlflow` added to `requirements-dev.txt` as a dev-only dep. Verified: all four scripts compile, `--help` shows the flags, and the enabled path was smoke-tested end-to-end (params/metrics/artifacts land in `mlruns/`) plus the two no-op paths (flag off; flag on but mlflow missing). Documented in `COMMANDS.md` (new "MLflow Experiment Tracking (debug, optional)" section + pointers in the talc/grain training blocks) and `SMOKE_TESTS.md`.

- Recorded the Nornickel VM stop request in `docs/ui/v2/notes/2026-07-04-nornikel-vm-stop-request.md`: Nornickel asked teams to stop using models on their servers, so the public VM must not be redeployed or used. Attempted to SSH to `team123@111.88.145.15` with the provided key and to probe ports `22`/`8080` from both the Mac and `docker-srv`; all timed out, so Docker containers could not be stopped from our side. The public UI endpoint is currently unreachable from our side.

- Benchmarked the trained talc SegFormer-B0 model on full held-out reviewed images. Added `scripts/benchmark_talc_model.py`, which loads each image-level fold checkpoint once, runs tiled full-image inference on its validation samples, clips predictions to analyzed non-sulfide pixels, and reports pixel IoU/F1, Hausdorff/HD95, and talc-fraction error against reviewed masks. Full 42-sample MPS run output is under `outputs/benchmarks/talc_segformer_b0_full_image_20260704`: SegFormer-B0 full-image talc IoU `0.6410`, F1 `0.7812`, versus blue-line baseline IoU `0.1577` / F1 `0.2725`; talc-fraction MAE is still `8.551` pp, so the model is stronger than the draft masks but not yet good enough for a strict ±3 pp talc-fraction claim. Saved the benchmark in `docs/benchmarks/02_talc_model_benchmark.md`.

- Added a **grade-classifier CNN branch** as a parallel branch to the segmentation-first pipeline: `scripts/train_grade_classifier.py` (torchvision `efficientnet_b3`, ImageNet-pretrained, class-weighted CE, AdamW cosine+warmup, AMP; no new deps — timm/albumentations avoided) + `scripts/evaluate_grade_branch.py`; plan `docs/plans/37_grade-classifier-cnn-branch.md`. Trained on gx10 (GB10). Because the fixed 345 eval split consumes all deconflicted talcose (0 left to train) and the talc segmentation does not identify the оталькованная grade (verified: `talc_fraction` on talcose ≈ 0, one-vs-rest F1 = 0 at any threshold), the CNN classifies **ordinary ↔ fine only**; talcose is deferred to the talc-seg branch. Training pool = manifest ordinary/fine minus the eval split (+ sha256 duplicates + label conflicts), deduped, grouped train/val split. **Held-out test on the 230 ordinary/fine of the 345 split (excluded from training): macro-F1 0.9303** (ordinary F1 0.933 P0.896/R0.974, fine F1 0.927 P0.971/R0.887; confusion ordinary [112,3], fine [13,102]); internal-val best 0.955. This lifts our learned ordinary/fine from the feature-CV's ~0.72 to ~0.93; it is a 2-class number (talcose pending). Artifacts under `models/grade_classifier/effb3_ordfine_20260704/`. Data gotcha fixed en route: gx10 had only the 345 eval images synced, so the first run crashed on missing training files — full dataset (2.8 GB) rsynced to gx10 and the training script now skips missing files defensively.
- Added the **grain-level intergrowth-classification advantage** to the jury deck: `presentation/presentation_ru.md` slide 6 gains an explicit callout (per-component `ordinary_intergrowth`/`fine_intergrowth` = true grain-level classification vs an image-level CNN's single holistic label) and `presentation/features_ru.md` the analysis bullet; both `presentation.html` and `features.html` re-rendered.

- Added v2 UI user-facing documentation: `docs/ui/v2/ore-pipeline-ui-user-guide.md` explains how the Workspace, Series, History, Settings, Status, API, viewer, editing, exports, and artifact model work; `docs/ui/v2/ore-pipeline-ui-customer-journeys.md` captures customer journeys for lab operators, reviewers, supervisors, admins, API users, and demo presenters. The v2 UI docs index links both files.

- Tested v2 UI Series bottlenecks with a live ML Series and an isolated heuristic control. The live ML Series on three 2272 x 1704 official JPGs created/started quickly but was canceled after 229 s because the first SegFormer-B2+talc ML child run had not completed under concurrent `run_resident_batch.py` load; the remaining two items stayed queued, confirming strict sequential execution. The same three-image Series completed with heuristic backends in 34.841 s, showing Series orchestration is not the dominant bottleneck. Evidence and next fixes are in `docs/ui/v2/notes/2026-07-04-ore-pipeline-series-bottleneck-test.md`.

- Added per-grain sulfide result inspection to the v2 ore pipeline UI. Completed run payloads now include `sulfide_grains` parsed from `reports/component_features.csv`, persist an RGB-encoded `masks/sulfide_component_labels_rgb.png` label map, and the result panel shows a sulfide-grain table after metrics with type, pixel area, and sulfide-area share. Checking one or more rows draws a combined cyan outline over the current image view using the existing pan/zoom/side-by-side transform. After the grain table, the UI now shows a technical run-details widget from `run.json`/`reports/runtime.json`: run id/status/stage, elapsed time, effective backend/model sources, ML checkpoints only for ML stages, tile dimensions/progress, stage outputs, and mask/report artifacts. Focused ore pipeline web tests cover the payload, label-map artifact, file listing, technical-details UI hooks, and grain UI hooks.
- Enriched the v2 sulfide-grain table into an OM-derived ore-grain report surface. New component CSVs include `perimeter_px`; run payload grain rows now add equivalent diameter, perimeter fallback for older CSVs, sulfide area share, matrix/talc/other-contact pixel counts, association percentages, `liberation_proxy`, and `locked_composite_proxy`. The UI adds compact RU/EN columns for those fields and labels liberation/contact/locked-composite values as OM-mask proxies, not chemistry-based MLA analysis. Focused `test_ore_pipeline_grain_reporting.py`, the existing ore web suite, and the ore UI browser smoke pass.

- Wired the trained talc segmentation model into the ore pipeline runtime. The main UI/runtime now has separate talc source, checkpoint, and threshold settings; `scripts/run_ore_pipeline.py` accepts `--talc-checkpoint/--talc-threshold` and writes `ml_pipeline/talc_model/` outputs; runtime provenance records `checkpoints.talc` plus talc checkpoint metadata; `/api/status` and the Status page show the active binary sulfide and talc model/source; and `POST /api/runtime/test` probes selected ML checkpoints per model. Focused `test_ore_pipeline_web.py` and REST API tests pass.

- Updated v2 ore pipeline run timing/progress UI. Runs now persist and expose `elapsed_seconds` from queued/start through complete, failed, or canceled terminal states; the workspace run status line shows elapsed time next to ETA; and `/history` no longer renders a progress bar, using a text-only percent plus muted stage/tile/ETA/elapsed details instead. Focused elapsed/progress tests and the full `tests.test_ore_pipeline_web` module pass.

- Added **resident (single-load) batch inference** to remove the pipeline's dominant fixed cost: the per-image Python-process spawn + 313 MB checkpoint reload. New `src/ore_classifier/resident_pipeline.py` (`ResidentSulfidePipeline` loads the model once; `run_image()` reproduces `run_ore_pipeline.py` in-process) and `scripts/run_resident_batch.py` (drop-in for `run_official_batch.py`, reuses `build_summary_row`/`write_batch_outputs` → identical `summary.csv`); opt-in `--resident` flag on `evaluate_official_pipeline.py` (default off, so the concurrent robustness ladder was unaffected). Additive only — no per-image script modified. **Parity (gate passed):** MPS 100.0000% sulfide-mask agreement + identical grade/fractions on 3 imgs; gx10 CUDA 15/15 predicted-class + sulfide_fraction match. **Speedup (gx10 GB10 A/B, same 15 imgs back-to-back):** subprocess 89 s vs resident 20 s = **4.45× wall**; inference-only 3.44 s→0.78 s/img (the subprocess timer includes the per-image reload resident removes). Extrapolated full 345 on gx10: ~34 min → ~7–8 min. Spec `docs/specs/resident-batch-inference.md`, plan `docs/plans/38_resident-batch-inference.md`, results `docs/notes/2026-07-04-robustness-and-resident-inference-results.md`. (gx10 env: NGC `nvcr.io/nvidia/pytorch:25.11-py3`, torch 2.10/CUDA 13, GB10; deps `transformers==5.13.0`+`opencv-python-headless`; repo+checkpoint+345 split images synced to `ashmelev@192.168.86.14`.)
- Ran the **robustness ladder** (V1 mild artifacts → V2 +color/blur/noise → V3 V2+preprocessing → V4 heavy+preprocessing) via the harness's JSON augmentation/preprocessing on the full 345 split. Feature-CV macro-F1 is robust: baseline 0.7467 → V1 0.7456 (−0.001) → V2 0.7239 (−0.023) → V3 0.7375 (−0.009) → V4 0.7169 (−0.030); worst-case (heavy adversarial artifacts + full preprocessing) costs only ~3 F1 points. Color/noise hurts more than surface artifacts; UI preprocessing net-stabilizes color/noise (V3 recovers V2). The deterministic rule metric (0.16–0.21, talcose≈0) is near-chance and moves non-monotonically under perturbation, so it is not a reliable robustness signal. Configs `outputs/robustness/configs/`, driver `outputs/robustness/run_ladder.sh`, results in the note above. Also corrected the augmentation JSON schema in `COMMANDS.md`/spec (`surface_artifacts.scratch_count/...`, not `acquisition.scratches`).
- Built **path B — the grain-level human-in-the-loop grade classifier** (interpretable, segmentation-first alternative to path A's whole-image `efficientnet_b3`, which is developed in a separate session — `scripts/train_grade_classifier.py` untouched). Reuses the strong sulfide segmentation (IoU ≈ 0.97) as a grain extractor: `scripts/build_grain_dataset.py` walks a completed official batch and exports per-grain crops + `grains_manifest.csv` (morphology features + heuristic pre-label + specimen group + grade) — 14,443 grains from the 345-image baseline. `apps/grain_review_web.py` is a stdlib browser app (cloned from the `talc_review_web.py` architecture) to human-label grains ordinary/fine/uncertain over the pre-label, persisting `annotations.json` keyed by `grain_uid`; smoke-tested (home/page/crop/annotate/persist/path-traversal-block all pass). `scripts/train_grain_classifier.py` trains a tabular classifier (ExtraTrees/RF/logistic) with **GroupKFold by аншлиф** (`src/ore_classifier/specimen.py`, shared helper aligned with path A's convention + a parent-folder scoping fix for ч2 counter collisions) and exports `model.joblib` + metadata. `scripts/aggregate_grade_from_grains.py` does a **leak-free nested grouped CV**: per fold it trains the grain model on train-image grains only, computes area-weighted fine-fraction per image, fuses the talc branch, calibrates (τ_fine, τ_talc) on train images, and predicts test-image grades — reporting image-level grade F1 comparable to the harness. Shared feature/model logic in `src/ore_classifier/grain_features.py`. Tests: `tests/test_grain_pipeline.py` 11/11 (specimen grouping incl. ч2 collision, feature engineering, label resolution, grade rule, crop geometry). v0.1 **bootstrap** result (heuristic grain labels, no human labels yet): grain grouped-CV macro-F1 0.998 (tautological — re-learns the heuristic), image-grade leak-free macro-F1 **0.1895** (≈ deterministic rule). **Key finding:** talcose F1 = 0 because the auto-candidate `talc_fraction` is ≈0 for talcose images (median 0.0000, 0 images > 0.02) — the talcose branch must consume the trained talc segmentation model, not the colour auto-candidate; that plus real human grain labels are the v0.2 gains. Spec `docs/specs/grain-human-in-the-loop-classifier.md`, plan `docs/plans/39_grain-human-in-the-loop-classifier.md`, commands in `COMMANDS.md`, presentation slide 17 + features entry added and `presentation.html`/`features.html` re-rendered.

- Fixed order-dependent flakiness in `tests/test_ore_pipeline_web.py`. Every test built its own `OrePipelineStore` but all pointed at the same fixed workspace `outputs/test_ore_pipeline_web/workspace`; runs launch on daemon threads (`run_async=True`), so a worker leaked from one test kept writing run artifacts into the shared path and the next test's fresh store then listed stray/half-written runs — producing nondeterministic failures whose set changed run to run (e.g. `test_delete_batch_endpoint_removes_series_and_child_runs` hitting `TypeError: PosixPath / NoneType` from a half-written `run.json`, plus `test_status_payload…`, `test_cancel_run…`, etc.). `setUp` now allocates a unique per-test workspace via `tempfile.mkdtemp(prefix="test_ore_pipeline_web_")`, so each store's runs/uploads/batches are private and a leaked worker can only touch its own discarded directory (`tearDown` keeps `rmtree(..., ignore_errors=True)`). Verified stable: the file passed 8/8 consecutive runs (previously failed ~1 in 2–3) and the full suite passed 150/150 across 3 runs. No production code changed.

- Enabled Git LFS for model checkpoints. The LFS filter driver had never been installed in this repo (`git config filter.lfs.*` was empty), so a first `git add` staged raw binaries; ran `git lfs install --local`, then added `.gitattributes` tracking `models/**/*.pt` and force-added the 5 curated trained checkpoints (`best.pt` + `last.pt` each, ~1.2 GB total) under `models/binary_sulfide` + `models/talc_segmentation` as LFS pointers, plus their small companion metadata (args/metrics/train_log/README/console logs). `models/*` stays gitignored (per user choice: leave `.gitignore`, force-add); only these paths were added. Verified each committed `.pt` blob is a ~133-byte pointer while the working-tree files remain the real binaries. **Pushed** to `origin/project-audit-and-fixes` (new branch): all 10 LFS objects (1.3 GB) uploaded successfully. GitHub retired the old pre-paid LFS data packs in favor of metered billing; the current free included allowance is **10 GiB storage + 10 GiB bandwidth** for Free/Pro personal accounts, so this ~1.2 GB payload fits the free tier — no data pack or payment needed. (Earlier notes citing a 1 GB free quota / $5 data pack referenced GitHub's now-obsolete model.)
- Added an end-to-end official evaluation & robustness harness: `scripts/evaluate_official_pipeline.py`. One command runs OUR pipeline over the official dataset and prints OUR metrics on the leak-free deconflicted split — dataset → manifest → label audit → deconflicted balanced split (`--exclude-conflicts --dedupe-sha256`, which drops the "images present in multiple variants": 48 sha256 label-conflict paths + 32 duplicate paths, leaving 345 images = 115/class) → `run_official_batch.py` → both evaluators → combined `metrics_summary.{json,md}`. Prerequisite artifacts are auto-built if missing, reused otherwise. Requirement #5 (robustness): `--augmentation-json` and `--preprocess-json` accept a file path or inline JSON and perturb every split image (augmentation → preprocessing, geometry-preserving, verified) into `<out-dir>/transformed_dataset/` before inference, using the same transforms as the browser UI. To guarantee that parity, extracted preprocessing into `src/ore_classifier/preprocessing.py` (`apply_preprocessing`, `normalize_preprocess_settings`, `preprocess_image`, panorama constants) and made `apps/ore_pipeline_web.py` import from it (`normalize_settings_preprocess` is now a thin `ApiError` wrapper); `apply_preprocessing` is the same object in both. Regression: `tests/test_ore_pipeline_web.py` + `tests/test_augmentation.py` = 39/40 (the one failure, `test_page_exposes_required_controls`, is pre-existing — the asserted JS string is absent at HEAD too and unrelated to this change). Smoke: `--skip-inference` reproduces the reference metrics; transform path verified geometry-preserving. Docs: spec `docs/specs/ore-pipeline-eval-harness.md`, plan `docs/plans/36_official-pipeline-eval-harness.md`, run/timing in `COMMANDS.md` (~50–60 min for a full 345-image run on this Mac, MPS, batch-size 1).- Added in-browser UI smoke tests (Playwright + headless Chromium) for both browser apps under `tests/browser/`. `tests/browser/conftest.py` boots the real stdlib `OrePipelineHTTPServer`/`TalcReviewHTTPServer` on an ephemeral port in a daemon thread (each with an isolated `tmp_path` workspace) and provides a `page` fixture that records JS console errors / uncaught exceptions; the whole browser suite is `pytest.importorskip`-guarded and skips the browser fixture if Chromium isn't installed, so `pytest tests/` still passes (skips, never errors) when Playwright/Chromium is absent. `test_ore_pipeline_ui_browser.py` (9 tests): home load with zero console errors, navigation across all five pages (`workspace/batch/history/settings/status/api`), API-docs rendering, and a full single-image **upload → heuristic run → results render** flow (`#fileInput` set_input_files → `#startBtn` → poll → `#resultPanel`/`#metricsTable`/`#textOutput` populated + history append). `test_talc_review_ui_browser.py` (3 tests): page load + sample-queue population, auto-load of the first sample into the viewer, and tool-switch revealing the similar-region params. Added `requirements-dev.txt` (pytest, playwright; run `python -m playwright install chromium` once). 12 browser tests pass and are stable across repeated runs — this supersedes the earlier "no headless browser available in this environment" note below. Separate pre-existing issue observed while running the full suite: `tests/test_ore_pipeline_web.py` is flaky in isolation (its tests share the fixed `outputs/test_ore_pipeline_web` workspace and spawn async daemon-thread jobs that outlive each test and race the next `rmtree`); the new browser tests avoid this by using isolated `tmp_path` workspaces.

- Fixed a v2 ore pipeline UI bug where a result-affecting parameter change made *after* pressing `Apply` (Augmentation/Preprocessing) but *before* `Start` was silently dropped. Previously `Apply` on a completed run snapshotted the current settings into an immutable `prepared` run, and `Start` on that prepared run sent an empty body so the backend reused the Apply-time snapshot — so a subsequent slider/checkbox change that was never re-`Apply`ed did not reach the new run, violating "the made changes must be applied". `Start` now always applies the current controls: `apps/static/ore_pipeline_ui.html` tracks a signature of the augmentation+preprocessing controls at `Apply` time (`state.preparedSettingsSig`, keyed by run id), and on `Start` of a `prepared` run it detects a settings drift (or unknown provenance, e.g. a prepared run restored from history) via `preparedRunSettingsAreStale()` and re-prepares the run *in place* (`POST /api/runs/{id}/prepare` with the live settings, no duplicate history entry) before starting. The direct "completed run → change param → Start (no Apply)" path already applied changes and is unchanged. Verified against a fresh live instance by driving the exact HTTP sequence the fixed handler emits: Apply(brightness=25) → change to 45 without re-Apply → Start re-prepares in place (same run_id) and the completed run's `augmented.png` reflects brightness=45 (mean 147.9 vs ~131 for the dropped v1); the edited inline script passes `node --check`. Added a real Playwright/Chromium regression test `test_param_change_after_apply_without_reapply_is_applied_on_start` in `tests/browser/test_ore_pipeline_ui_browser.py` that drives the actual DOM click-path (upload → Start → open Augmentation, set brightness=10, Apply → change slider to 45 without re-Apply → Start) and asserts, via the browser's own fetch, that the completed immutable run stored brightness=45 (not the stale 10), that Start re-prepared the run in place (same run id), and that no duplicate history entry was created — console/pageerror-clean and stable across repeated runs. Installed `playwright`/`pytest-playwright` + Chromium into `.venv` to run it; the browser suite (`tests/browser/`) stays collection-safe and skips when Playwright/Chromium is absent (`conftest.py` uses `importorskip`). Full suite now `164 passed`. The backend in-place re-prepare path is also unit-tested in `tests/test_ore_pipeline_web.py`.

- Built the jury presentation package under `presentation/`: Russian slide deck content (`presentation_ru.md`, 17 slides + appendix, Lenta-style heading→text→speaker-note), a separate look file (`theme/deck.css`, dark microscopy theme with semantic class colors), a self-contained renderer (`render_presentation.py`: Markdown + CSS → one offline HTML with images embedded as downscaled data-URIs), a standalone features one-pager (`features_ru.md` → `features.html`), and a Playwright capture script (`capture_screens.py`). Rendered `presentation.html` (2.6 MB) and `features.html`. The deck covers: task/why, interpretable-pipeline principle, datasets (official 1180 image-level + 14 panoramas + 42 blue-line talc; LumenStone S1/S2 proxy), training approach & rationale (weak supervision, binary-first, agreement/ignore), the sulfide SegFormer-B2 model + weak-label metrics table, component ordinary/fine, the talc detector, the deterministic ore rule, tiled panorama inference, the talc review app (`talc_review_web.py`, incl. what line 2784 is), the main app (`ore_pipeline_web.py`), a features section, honest metric-vs-target status, reproducibility/Docker, and next steps. Screenshots of both live apps + pipeline artifacts captured via Playwright (heuristic/ML runs already in `outputs/ore_pipeline_ui/`). Ports 8230/8231 used only transiently for capture.
- Corrected a factual error in `docs/notes/2026-07-03-pipeline-improvement-proposals.md` item 7: the deployed SegFormer-B2 was fine-tuned from the ImageNet-pretrained `nvidia/mit-b2` backbone (verified via its `console.log`), NOT trained from random init as previously claimed; the presentation reflects the corrected fact.
- Playwright QA pass on both browser apps (ore pipeline `apps/ore_pipeline_web.py`, talc review `apps/talc_review_web.py`), findings in `docs/ui/v2/notes/2026-07-04-playwright-ui-qa-issues.md`. Drove all 7 ore routes, end-to-end upload→run→metrics→exports→history, Fix-me editor, View-files popup, RU↔EN/theme toggles, API sandbox, Series/Settings, plus the talc canvas/toolbar — console/network clean throughout. Fixed two issues: (1) [medium] the `metrics.csv`, `report.pdf`, and Series `results.csv` download endpoints sent no `Content-Disposition`, so "Save" navigated the SPA away / opened the PDF inline with no filename — now pass `download_name=` to `send_file` (mirroring the already-correct `artifacts.zip`), verified they return `attachment; filename*=…`; (2) [low] run-progress stage labels leaked raw English into the default-Russian UI because `stageLabel()` didn't map the backend stages `preparing immutable run artifacts` / `ordinary/fine intergrowth and talc analysis` / `running ML tiled inference` / `collecting ML outputs` / `building display layers` — added keyword branches plus `stagePreparing`/`stageAnalysis`/`stageMlInference`/`stageCollecting`/`stageDisplayLayers` i18n keys (RU+EN), verified all six now map to Russian. `test_ore_pipeline_web.py` 39/39 pass. Documented but not fixed: switching language after a completed run doesn't re-localize the final progress caption (edge case).

- Project audit + fixes. Wrote `docs/notes/2026-07-03-project-audit-issues-bottlenecks.md` (ranked issues/bottlenecks/gaps) and applied the safe fixes: (1) rebuilt `.venv` on Python 3.12 — the committed venv was Python 3.9.6, which fails the code's `zip(strict=True)`/PEP 604 usage and could not run the suite; (2) reconciled `requirements.txt` (the old `transformers>=5.12.1` floor contradicted the only verified runtime, `transformers 5.5.4`) to `>=5.5.4`, added the previously-undeclared `scikit-learn` import dependency, and generated `requirements.lock` (`pip freeze`: torch 2.12.1, transformers 5.13.0, numpy 2.5.0, opencv 5.0.0.93, scikit-learn 1.9.0, …) as the reproducible source of truth; (4) extracted the ~5,700-line `HTML_PAGE` raw string out of `apps/ore_pipeline_web.py` into `apps/static/ore_pipeline_ui.html` — the file dropped 10,892→5,224 lines and `render_html_page()` now loads the page from disk once (cached); byte-exact (served bytes match the static file) and all routes verified; (7) added `scripts/clean_smoke_outputs.sh`, a dry-run-by-default reclaimer for disposable `outputs/*smoke*`/`*probe*`/`pycache_tmp` dirs (currently ~838 MB reclaimable). Full suite `136 passed` on the new venv.

- Performance: profiled the v2 ore pipeline hot paths. `list_runs` is fast (~6 ms/18 runs); `status_payload`'s ~2.2 s cost is a one-time cold `system_profiler` GPU probe already `@lru_cache`d process-wide, so warmed `/status` is ~40 ms (not an ongoing bottleneck). The dominant ongoing bottleneck is full-resolution preprocessing on the synchronous upload request thread — `apply_preprocessing` with the default preset (denoise ON via `cv2.fastNlMeansDenoisingColored`) measured ~12 s on a ~24 MP image (denoise alone ~6.5 s) vs ~1.5 s at 2600 px analysis scale; because the non-deferred path feeds analysis via `downscale(preprocess(full))`, cutting it changes analysis results and needs a product decision (recorded in the audit note). Applied the one provably safe win now: refactored `apply_preprocessing` to keep a single numpy RGB buffer across steps instead of round-tripping PIL↔numpy per step — verified pixel-identical (maxdiff=0 across presets), reducing peak memory (fewer full-size copies) with no output change.

- Pre-generated the demo-video `/history` runs so recording loads instantly, and added `scripts/generate_demo_history_runs.py` (REST driver: multipart `/api/uploads` → `/api/runs/start` → poll to `complete`). Final curated set in `outputs/ore_pipeline_ui`: ML B2 singles `2544791-2 10x аншлиф` (`row_ore`, sulf 0.748) and `2548045 10x 1` (`hard_to_process_ore`, sulf 0.272), ML B2 panorama `13.jpg`, plus heuristic fallbacks for the row single and panorama; each verified to classify correctly through the running app, run IDs recorded in `DEMO_SCRIPT.md`. Two constraints found and documented: (a) the web app resolves backend from persisted settings — the `--backend` launch flag is overridden — so backend is switched via `PUT /api/settings` (persisted setting left at ML B2); (b) the web app has no calibrated-rule or reviewed-talc-mask hook (CLI-only), and on the official split 0/115 talc images classify as `talcose_ore`, so the demo tells the talc story via `apps/talc_review_web.py` reviewed masks (e.g. `2550381-1 10x`) instead of the main pipeline's weak auto-candidate. Heuristic misclassifies the fine-intergrowth single (sulf 0.099 → `row_ore`), so singles are featured on ML B2.

- Added `DEMO_SCRIPT.md` (repo root): a shot-by-shot ~5-minute submission demo-video plan (problem → data/leakage → architecture/metrics → live single-image app → panorama → deployment). All spoken numbers are pinned to an honest fact box sourced from `ChangeLog.md`/`docs/benchmarks/01_binary_sulfide_model_benchmark.md` (B2 sulfide IoU 0.974/F1 0.987, image-level macro F1 0.744/AUC 0.880, talc mean IoU 0.644, 56 duplicate / 24 conflict-label groups, group-level split, offline Docker deploy), with the weak-supervision caveat and the CPU-bound live-panorama recording risk called out. Added Immediate Next Step 21 in `docs/session-sync.md` pointing to it.

- Reviewed the test suite and filled the largest untested-logic gaps. Added `tests/test_datasets.py` (7 tests for `BinarySulfideTileDataset`: split filtering, missing-split `ValueError`, ignore/mask/background target encoding to 255/1/0, JSON meta round-trip, image tensor shape/range, ImageNet normalization, augmentation label preservation), `tests/test_analyzed_area.py` (5 tests covering non-RGB rejection, dark-border and blue-markup exclusion, internal saturation derivation, and `ellipse_kernel` sizing), and extended `tests/test_talc_candidate.py` with 4 tests for `talc_candidate_overlay` (blue talc tint, aspect-preserving downscale, yellow sulfide tint) and `save_talc_candidate_outputs` (artifact writes + summary fraction math). Coverage of `src/ore_classifier`: `datasets.py` 30%→90%, `talc_candidate.py` 53%→91%, `analyzed_area.py` 91%→100%, overall 74%→78%. Full suite now `138 passed`. Note: running the suite requires `scikit-learn` installed (imported by `scripts/evaluate_ore_feature_classifier.py`, which raises a module-level `SystemExit` on collection when absent); it is not yet listed in `requirements.txt`.

## 2026-07-03

- Added repo-root launch scripts `run_main_app.sh` and `run_talc_app.sh` for the two browser apps. `run_main_app.sh` starts the end-to-end ore pipeline UI (`apps/ore_pipeline_web.py`, heuristic backend, OS-assigned port); `run_talc_app.sh` starts the talc mask review app (`apps/talc_review_web.py`) against the prepared `outputs/talc_blue_line_conversion` workspace by default. Both prefer the repo-local `.venv`, `cd` to the repo root, pass extra args through, and support `ORE_*`/`TALC_*` host/port env overrides; the talc script skips its default `--conversion-dir` when the caller supplies `--conversion-dir`/`--annotated-dir`.

- Added a destructive Settings action to remove all v2 ore pipeline history. `/settings` now has a separate History panel with `Remove all history`, backed by `DELETE /api/history`, which deletes persisted run and Series artifact folders while preserving uploaded source images and app settings, and rejects the operation while jobs are active.

- Completed the full zelda SegFormer-B0 5-fold non-sulfide talc segmentation run and mirrored artifacts locally under `outputs/talc_segformer_folds/segformer_b0_full_20260703`. Mean calibrated talc IoU is `0.644191`, mean F1 is `0.782301`; launcher/logs are under `outputs/logs/`.

- Added sortable columns to the v2 ore pipeline Run files popup. Filename, type, size, and image-dimension headers are now clickable client-side sort controls while preserving per-file `View` links and ZIP download behavior.

- Added Apple Silicon GPU detection to the v2 Status page. When `nvidia-smi` is absent on macOS, `/api/status` now reads `system_profiler` once, reports the Apple Metal GPU, core/display counts, and PyTorch MPS availability, while leaving utilization/VRAM metrics blank because macOS does not expose NVIDIA-style counters there.

- Added a per-file `View` action to the v2 ore pipeline Run files popup. `/api/runs/{run_id}/files` now returns a `view_url` for each immutable artifact, and the popup renders a localized action column that opens the selected file through the existing `/artifacts/...` endpoint.

- Fixed the v2 ore pipeline Status page during large-image work. `/api/status` now reports foreground HTTP operations such as upload parsing, preview generation, preprocessing, and run preparation, and `/status` polls while visible so active-task and CPU/RAM cards update during long synchronous image operations.

- Added live progress to the v2 ore pipeline History page. `/api/runs` now overlays active in-memory job state onto persisted `run.json` rows, and `/history` shows a Status/Progress column with active run percent, stage details, ETA, and tile counts when ML tiled progress is available.

- Added run runtime provenance to the v2 ore pipeline UI. Every run now records `runtime` in `run.json` plus `reports/runtime.json`, including effective backend, binary sulfide checkpoint path when ML is used, explicit empty checkpoints for heuristic/rule steps, ML checkpoint metadata/device/tile settings when available, and talc/final segmentation runtime sources. File listing and ZIP exports include `reports/runtime.json`; focused `unittest` web coverage validates heuristic and ML provenance.

- Optimized large-image v2 ore pipeline preparation. Upload previews now decode at display scale, preview pyramids reuse downscaled intermediates and avoid expensive optimize passes, path uploads reuse one SHA-1 calculation, panorama-scale preprocessing defers full-size `preprocessed_full.png`, and ML tiled inference reports processed/total tile progress through `progress.json` into the run progress indicator. Local smoke on `dataset/Панорамы/13.jpg` prepared the `13330 x 9489` source as `1800 x 1282` analysis data in about `2.3 s` with `full_size_processing_deferred=true`. Details: `docs/ui/v2/notes/2026-07-03-ore-pipeline-large-image-performance.md`.

- Fixed the v2 sulfide viewer `non-sulfides` class visibility. `non-sulfides` now uses a generated `non_sulfide_base` masked display layer, so disabling `sulfides` no longer leaves sulfide pixels visible through the raw base image. Completed runs missing this derived display layer regenerate it on load.

- Added `scripts/run_talc_segformer_folds.py` for pretrained SegFormer talc image-level folds and threshold calibration; ran a local B0 smoke under `outputs/talc_segformer_folds/segformer_b0_smoke_20260703`.

- Added the non-sulfide talc segmentation training path: `build_talc_dataset.py` now ignores sulfide pixels by default, new `scripts/train_talc_segmentation.py` trains the talc binary model, new `scripts/infer_talc_segmentation.py` writes non-sulfide-clipped talc masks, and a local ResUNet baseline produced `outputs/talc_non_sulfide_dataset_v0`, `models/talc_segmentation/resunet_non_sulfide_20260703_local`, and a DSCN4714 prediction smoke.

- Cleared stale segmentation results immediately when pressing `Start` in the v2 ore pipeline UI. The client now hides the old text/metrics/export panel, removes old `sulfide`/`final` state from the viewer, resets side-by-side comparison to `none`, and switches back to the best available input layer before the new run request completes.

- Added `docs/notes/2026-07-03-ml-runtime-fix-work-summary.md`, a consolidated record of the ML runtime failure diagnosis, SegFormer checkpoint namespace remap, Runtime Test behavior, verification commands, current service state, and follow-up guidance.

- Fixed local ML runtime loading for zelda-trained SegFormer checkpoints. `src/ore_classifier/model_io.py` now strictly remaps compatible Transformers SegFormer `segformer.stages.*` checkpoint keys to the local `segformer.encoder.*` model namespace, records `state_dict_compatibility`, and refuses the remap unless every key and tensor shape matches. The Settings Runtime `Test` probe now resolves the same `auto` device as real inference. Verified B2 checkpoint load on CPU and MPS, B1/B0 checkpoint load on CPU, `/api/runtime/test` on `127.0.0.1:63589`, `infer_binary_sulfide.py` on the failed run input, and full `run_ore_pipeline.py` on the same input.

- Restored the v2 ore pipeline `Status` tab to the visible top menu. The route already existed, but the top-nav button was hidden with `hidden-status-tab`; it is now visible before `API` and `Settings`.

- Added a non-mutating Runtime `Test` action to the v2 ore pipeline Settings page. The new `/api/runtime/test` endpoint checks unsaved backend/checkpoint form values, returns immediate success for `heuristic`, and for `ml` runs a bounded subprocess probe through the same checkpoint loader used by pipeline runs without creating a run or saving settings.

- Increased the talc review Brush size slider maximum from `120 px` to `240 px`.

- Renamed the talc review toolbar tool from `Similar Talc` to `Similar` in user-facing UI text now that segmentation class selection is explicit; persisted edit identifiers remain compatible.

- Added a transparent top-right `Next` action to the talc review UI after `Save & Next`; it advances through the current visible queue without saving while keeping existing dirty/draft navigation warnings.

- Hid the model checkpoint path from the v2 Status backend card when the active backend is `heuristic`; the checkpoint is still shown for `ml` and remains editable in Settings for switching modes.

- Added `docs/ui/v2/ore-pipeline-docker-deployment-runbook.md`, a reusable deployment guide for both verified Docker targets: the public Nornickel VM (`team123@111.88.145.15:8080`, `linux/amd64`) and gx10 (`192.168.86.14:8210`, `linux/arm64`). The runbook covers remote staging/build, key handling outside the repo, image transfer, `docker run` commands, smoke checks, operations commands, and known panorama/GX10 caveats.

- Added per-class `Edit` radios to the talc review `SEGMENTATION CLASSES` widget. Brush, Fill, Rectangle, and Polygon now draw/erase the selected class (`Positive bag` or `Talc`), while class visibility checkboxes remain display-only.

- Added a live runtime backend switch to the v2 ore pipeline Settings page: `/settings` now persists `heuristic`/`ml` plus checkpoint path, validates ML checkpoints, applies changes immediately for new runs, blocks runtime changes while jobs are active, and snapshots backend/checkpoint into immutable `run.json` files.

- Built the v2 ore pipeline Docker image natively on gx10 as `nornikel/ore-pipeline-ui:v2-arm64`, launched `nornikel-ore-pipeline-ui-v2` on `192.168.86.14:8210` with `--gpus all`, fixed `/api/status` parsing for GB10 `nvidia-smi` `[N/A]` memory fields, and recorded ARM64 serve/run/performance evidence in `docs/ui/v2/notes/2026-07-03-ore-pipeline-docker-gx10-arm64-smoke.md`. Final normal-image smoke passed in about `18.6 s`; idle container usage was about `301 MiB` RAM and `0.01%` CPU.

- Fixed Similar Talc semantics for positive bags: `positive_bag` is now treated as a rough containing region, not confirmed talc. Similar Talc can preview/apply `talc_node` pixels inside a positive bag, and the talc-node overlay remains visible on top of the bag.

- Added an over-image talc segmentation class visibility widget with checked `Positive bag` and `Talc` toggles. It controls the existing positive-bag and talc-node overlay visibility without changing the saved class masks.

- Split the talc review working mask into two review classes: `positive_bag` for blue-line/manual Brush/Fill/Rectangle/Polygon/SAM2 edits and `talc_node` for Similar Talc output. The app now autosaves/reviews separate class masks plus the existing union `current_talc_mask.png` / `reviewed_talc_mask.png` for compatibility, and Similar Talc no longer relabels positive-bag pixels as talc nodes.

- Added a v2 ore pipeline `API` page at `/api` with localized REST documentation, endpoint navigation, request/response examples, multipart upload sandbox, JSON request sandboxes, and readable binary-download responses for PDF/ZIP exports. Documented the REST contract/plan under `docs/ui/v2/`, extended focused UI tests, and made repeated same-image uploads allocate unique IDs for fast batching/API use.
- Added a v2 ore pipeline `Status` / `Статус` page with direct `/status` route and `/api/status`: it reports CPU load, optional NVIDIA GPU utilization through `nvidia-smi`, RAM, Flash/disk usage, history size, run/series counts, active jobs, backend/checkpoint state, and overall health checks. Added v2 UI spec/plan docs and focused regression coverage.
- Extended the v2 ore pipeline Status page with bounded in-memory system and access logs: recent app events, run/series lifecycle events, request errors, and sanitized HTTP access entries now appear in `/api/status` and the `/status` UI.
- Expanded the v2 ore pipeline PDF report into a five-page lab-style artifact following `docs/notes/2026-07-03-public-ore-report-style-findings.md` from the original hackathon repo: a cautious demonstration-report title, `Паспорт исследования`, conclusion, `Результаты количественного анализа`, named photo-documentation pages, method/QC limitations, reproducibility notes, expert-review fields, and the existing original/preprocessed/sulfide/final/class-mask visuals. The `/report.pdf` endpoint regenerates the current report layout for existing runs and the rendered output was verified with Poppler.
- Changed v2 ore pipeline `Apply` behavior for Augmentation and Preprocessing: applying settings from a completed run now creates a new immutable `prepared` run with prerequisite artifacts rebuilt up to the changed step and downstream masks/metrics/reports cleared; applying again before `Start` updates the same prepared run, and `Start` continues that run in place. Added the v2 UI spec/plan and focused regression coverage.
- Fixed Similar Talc save behavior: `Save` and `Save & Next` now merge an active Similar Talc preview into the working mask before writing reviewed outputs, so visible preview results are not silently dropped if the reviewer skips the separate `Apply Similar` button.
- Fixed v2 ore pipeline PDF report generation from the browser UI: long Russian conclusion text and metric values now wrap inside the A4 page instead of clipping, run metadata JSON writes are atomic so async `/api/runs/start` responses no longer race with worker progress writes, and the UI download path was verified by multipart upload -> preprocess -> async run -> `/report.pdf` download -> Poppler render.
- Fixed `Similar Talc` positive-bag clicks so the clicked seed patch stays the anchor: nearby current-mask pixels are now filtered against that seed before they can influence luma/color statistics, strictness is tighter at `100`, and broad matrix-heavy bags no longer immediately trigger oversized whole-image previews.
- Re-ran the v2 ore pipeline browser UI smoke on `/workspace`, `/history`, `/history_series`, `/batch`, and `/settings`; focused ore/talc/Docker UI tests pass, and the remaining Edit & Recalculate statistics wrapping issue is recorded in the v2 UI QA note.
- Built the v2 ore pipeline Docker image on `docker-srv`, streamed it into the Nornickel VM, launched `nornikel-ore-pipeline-ui` on `team123@111.88.145.15:8080`, and recorded VM serve/run/performance evidence in `docs/ui/v2/notes/2026-07-03-ore-pipeline-docker-vm-smoke.md`. Normal official-image smokes pass; full panorama upload preparation is CPU-bound on synchronous full-size PNG generation and should be optimized before live panorama demos.
- Added the talc review `Similar Talc` canvas assist: a reviewer can click a confirmed talc grain, preview luma/color-similar non-sulfide pixels in yellow, tune `Strictness`, clear with right-click, and `Apply Similar` into the working talc mask with undo/autosave and auditable `similar_talc_add` edits.
- Added a dedicated DOM overlay for the v2 ore pipeline side-by-side splitter so the draggable divider remains visible above sulfide/final canvas overlays and segmentation legends.
- Added `Remove` to the v2 ore pipeline `/history_series` table. Series removal now uses `DELETE /api/batches/{batch_id}`, refuses active series, deletes the persisted series folder plus child immutable run artifacts, and refreshes History without leaving broken batch back-links.
- Added `/history_series` as the v2 ore pipeline History slug for the selected Series mode: direct-loading the slug opens History with Series selected, selecting Series from `/history` pushes `/history_series`, and browser back/forward restores the matching history mode.
- Fixed the v2 ore pipeline overlay segmentation legends so hidden sulfide/final class groups stay hidden and each visible legend renders as a single vertical class list.
- Moved the v2 ore pipeline segmentation class visibility legend from the toolbar onto the image view: the primary/left layer legend appears in the top-left overlay and the side-by-side/right layer legend appears in the top-right overlay, with duplicated controls synchronized through one shared class-visibility state.
- Kept v2 ore pipeline viewer toolbar segmented selector items on one line while preserving the no-horizontal-scrollbar behavior.
- Removed the decorative `<--->` divider before the v2 ore pipeline `Side-by-side` viewer selector.
- Moved the v2 ore pipeline Series `Add images` action into the `Gallery` panel and added per-card `Remove` for draft image cards through `DELETE /api/batches/{batch_id}/items/{item_id}`; started or completed immutable child runs remain non-removable from the card.
- Moved the v2 ore pipeline viewer-options row (`show tiling`, `contours only`, `opacity`) below the image canvas so it no longer occupies the top toolbar above the image view.
- Renamed the user-facing v2 ore pipeline multi-image workflow from `Batch` / `Пакеты` to `Series` / `Серии` across navigation, controls, history mode, and status messages while keeping the existing `/batch` routes and stored `batch_*` artifacts for compatibility.
- Added a per-run file browser beside the CSV/PDF exports in the v2 ore pipeline results: `View files` opens a localized popup listing every file in the immutable run directory with byte size and `WxH` dimensions for image files, and `Download ZIP` downloads the full run contents through `/api/runs/{run_id}/artifacts.zip`.
- Fixed large-image v2 ore pipeline preparation so runtime augmentation and preprocessing are applied to the original source dimensions before creating the downscaled analysis/display copy. Upload metadata now records full-size augmentation/preprocessing dimensions plus the analysis-scale `preprocessed.png`; immutable runs keep `input/preprocessed_full.png` when preprocessing is enabled. Verified the code path with a dry-run on `dataset/Панорамы/10.jpg` (`14999 x 10391` -> analysis `1800 x 1247`).
- Added contextual v2 ore pipeline viewer controls: original/augmented/preprocessed hide segmentation class toggles, sulfide shows sulfides/non-sulfides/artefacts, final shows ordinary/fine/talc/artefacts/background, side-by-side participates in the same toggle visibility logic, and tiling/contours/opacity live in a separate viewer-options row. Documented the behavior in `docs/ui/v2/notes/2026-07-03-ore-pipeline-contextual-viewer-controls.md`.
- Removed the separate v2 ore pipeline `artefacts` viewer tab and side-by-side option; artefact regions now render inside the `sulfide` view using the existing violet/magenta artefact visibility checkbox, and remain visible in final overlays.
- Reworked the v2 ore pipeline result panel so text output appears first and the metrics table sits below it at full width; the metrics table is now hierarchical: analyzed area, sulfides with ordinary/fine children, talc, other analyzed area, and image artefacts.
- Added an explicit `Apply` button to the v2 ore pipeline Augmentation panel. Like preprocessing `Apply`, it enables augmentation, saves the current settings, refreshes upload debug previews through the existing preprocess endpoint, and switches the viewer to the ready `augmented` layer.
- Added `docs/ui/v2/plans/36_ore-pipeline-source-disagreement-map.md`, a focused plan for replacing a plain confidence heatmap with a source-disagreement layer, review candidates, UI legend, optional teacher/TTA sources, and report/evidence artifacts.
- Re-reviewed the live v2 ore pipeline and talc review browser UIs, then refreshed `docs/ui/v2/TODO_CANDIDATES.md` with current-state ideas: result-layer toolbar fit, backend/checkpoint readiness, demo sample launcher, rule provenance, evidence bundle, review-candidate/source-disagreement surfaces, talc review queue semantics, and RU talc-review polish.
- Added an artefact visibility checkbox to the v2 ore pipeline viewer class-toggle row and recolored artefact overlays to a distinct violet/magenta color so they do not share the red/green/blue final segmentation palette; Edit & Recalculate now uses the same themed artefact color.
- Added a Docker runtime for the v2 ore pipeline GUI: `.dockerignore`, `docker/ore-pipeline-ui/Dockerfile`, entrypoint, Docker-specific requirements, `docker-compose.ore-pipeline-ui.yml`, v2 UI spec/plan docs, launch/smoke documentation, and focused artifact tests. The image defaults to the heuristic backend for VM demo reliability and excludes dataset, generated outputs, and model checkpoints from the build context.
- Added `docs/plans/35_talc-detector-training.md` and implemented its first step `scripts/build_talc_dataset.py`: a tiled talc/not-talc training dataset from the 42 human-reviewed masks with reviewed-ignore plus border exclusion (reviewed talc stays authoritative over the analyzed-area floor), per-image stratified train/val splits with `--val-samples` override, optional SHA-256-deduplicated pure-negative sources disabled by default, and a manifest consumable by the existing sulfide trainer; generated `outputs/talc_dataset_v0` (1029 tiles, 770 train / 259 val, 10 val images, median tile positive fraction 0.277) and added 7 focused unit tests (full suite 85 tests passing).
- Fixed the v2 ore pipeline UI/UX QA findings from `docs/ui/v2/notes/2026-07-03-ore-pipeline-ui-ux-qa-review.md`: non-workspace pages now hide the workflow sidebar, mobile viewer/header controls no longer overflow at 390px-class widths, loaded runs reset scroll to the top of Workspace, Edit & Recalculate opens on the relevant current/final layer, final stats and metrics label denominators, Russian-mode Batch/Augmentation/Pan labels are localized, disabled CTA buttons are neutral, result overlays have opacity and contour-only controls, result decisions show margin/rationale text, and the main canvas height adapts to image aspect ratio.
- Analyzed the completed manual talc mask review (all 42 samples now have `reviewed/reviewed_talc_mask.png`): a median 83% of reviewed talc lies outside the original blue-line bags (annotations were examples, not exhaustive), reviewed talc fractions are median 0.274 of analyzed area, an oracle per-image luma threshold reaches median IoU 0.502 against the reviewed GT, and the production HSV talc candidate scores median IoU 0.000 and must be replaced; analysis CSV/scripts under `outputs/talc_luma_enrichment/`, findings and implications recorded in `docs/notes/2026-07-03-talc-ground-truth-strategy.md`.
- Added `docs/ui/v2/notes/2026-07-03-ore-pipeline-ui-ux-qa-review.md`, a live UI/UX QA review of the v2 ore pipeline app covering mobile overflow, non-workspace layout width, run-load scroll reset, editor default layer, metric denominators, Russian localization gaps, disabled button affordance, overlay readability, and decision-rationale needs.
- Added `B` and `F` keyboard shortcuts to the talc browser review app for selecting Brush and Fill, with text-field and modifier-key guards.
- Updated the talc browser review `Fill` tool so loaded sulfide pixels are hard flood-fill boundaries alongside blue strokes, existing talc mask regions, and image edges.
- Quantified the "talc is the darkest phase" luma-threshold hypothesis over the blue-line conversion workspace: dark pixels are enriched inside talc bags in 26/28 analyzable samples (median 2.15x, no global threshold exists), but a median of only 12% of dark-flake area lies inside the bags, so annotation exhaustiveness is now the decisive open question; analysis scripts, CSV, and an in-bag vs out-of-bag flake gallery are saved under `outputs/talc_luma_enrichment/`, and `docs/notes/2026-07-03-talc-ground-truth-strategy.md` records the finding and implications.
- Added a talc browser review `Fill` tool that flood-fills the clicked connected area bounded by raw/closed blue annotation strokes, existing talc mask regions, and image edges; fill edits are undoable, autosaved, and still respect sulfide protection.
- Separated panorama scaling from the main preprocessing-default checkbox row on the v2 Settings page: normalization, denoise, and contrast stay grouped above a divider, while `масштабирование для панорамных снимков` and its mode/value controls are in their own scale group with regression coverage.
- Added History page modes to the ore pipeline UI: all runs, standalone single runs, and batches; batch history rows open the existing `/batch/{batch_id}` details page, while batch child runs are excluded from the single-run mode.
- Added a non-destructive luma-threshold dark-pixel preview slider to the talc browser review app: `255` is off, `90` is a quick talc-candidate preset, brighter pixels are painted white using `0.299*R + 0.587*G + 0.114*B`, and the selected value is stored in autosave/review patch view metadata.
- Added `docs/notes/2026-07-03-talc-ground-truth-strategy.md`: strategy note recording that blue-line talc annotations are region-level rather than pixel ground truth, with proposed GT sources (point counting for fractions, PU/enrichment tri-map labels, dual-annotator silver set, organizer clarifications) and a detection-approach ladder; no implementation started yet.
- Added the ore pipeline main viewer `artefacts` layer between `augmented` and `preprocessed`, moved background/ordinary/fine/talc visibility controls directly under the primary layer selector, and exposed artefacts in side-by-side comparison when the mask overlay exists.
- Added mouse-wheel / middle-button drag panning to the talc browser review canvas, independent of the active Brush/Rectangle/Polygon/SAM2 tool, with regression coverage and smoke checklist coverage.
- Centralized v2 UI documentation under `docs/ui/v2/`: ore pipeline UI plans/specs, talc review UI docs, v2 UI notes, and `TODO_CANDIDATES.md` now live under the same docs folder, references were updated away from the old `docs/plans/` and `docs/specs/` paths, and `AGENTS.md` now tells future agents to keep new v2 UI docs there.
- Made v2 ore pipeline panorama preprocessing explicit: the preprocessing popup and Settings page now expose `масштабирование для панорамных снимков` as off/longest-side-bound/scale-factor controls, summaries show the active rule, run metadata records the applied target size, and tests cover max-side/factor/off behavior while keeping tiling independent.
- Added `docs/ui/v2/plans/34_ore-pipeline-augmentation-misses.md`, a v2 UI/training plan listing the remaining augmentation misses: shared training/runtime profiles, artifact-mask review/export, reviewed talc-mask attachment, rule/calibration visibility, warning/robustness surfacing, training-feedback export, and reproducible augmentation review evidence.
- Added artefact-mask editing to the ore pipeline UI: `Fix me` is available after image upload, the Edit & Recalculate dialog now has an `Artefacts` layer with a red brush, pre-run artefact masks save to the upload for the next `Start`, post-run artefact edits create immutable derived runs, and marked pixels are excluded from sulfide, talc, final masks, and metric denominators.
- Added `scripts/generate_augmentation_review_gallery.py` and generated `outputs/augmentation_review/index.html`, a static review gallery with one deconflicted official sample per class and nine deterministic v2 runtime augmentation variants per sample, including exact settings JSON for each card.
- Added scale-aware metrics to the v2 ore pipeline UI: calibrated `microns_per_pixel` or `pixel_size_um` metadata now produces pixel and physical areas in `run.json`, the result metrics table, `reports/metrics.csv`, and PDF report values, with scale provenance recorded per run. Added `docs/ui/v2/specs/ore-pipeline-scale-metrics-v0.1.md`, `docs/ui/v2/plans/33_ore-pipeline-scale-metrics.md`, and focused regression coverage for `microns_per_pixel`.
- Added `docs/ui/v2/TODO_CANDIDATES.md`, a ranked v2 ore pipeline UI candidate backlog covering demo readiness, backend health, calibration/rule visibility, feature-classifier comparison, warning surfacing, reviewed talc-mask attachment, artifact bundles, batch/history hardening, review queues, and explicit out-of-scope boundaries.
- Fixed the talc browser review UI audit findings: sample cards and subtitles now show human-readable statuses instead of raw enums, autosave has explicit saved/saving/error working-mask state, `Save & Next` follows the active filtered queue, unsafe sample switching warns on unfinished/failed local edits, selected/missing layers report visible warnings, the toolbar has responsive layout/ARIA state/cursor affordances, zoom shows percentage and wheel zoom anchors under the cursor, and focused talc UI tests cover the updated contract.
- Performance-checked the talc browser review app on the local `outputs/talc_blue_line_conversion` workspace: `/` and `/api/manifest` are sub-millisecond median responses, `/api/samples` is about `84 ms` p50 / `96 ms` p95 across all 42 samples, largest per-sample initial asset groups are about `8 MiB`, and concurrent first-open current-mask creation is now serialized to avoid transient `500` responses under parallel sample loads.
- Implemented the v2 ore pipeline `Batch` page in `apps/ore_pipeline_web.py`: `/batch` and `/batch/{batch_id}` are direct-loadable, multiple uploaded images are grouped under `outputs/ore_pipeline_ui/batches/`, per-image `Edit Metadata...` is stored before execution, preprocessing and runtime augmentation settings are shared across the batch, `Run Batch` processes images sequentially into ordinary immutable runs, per-card progress and `Load` links are shown, and loaded run results can return to the originating batch; added `docs/ui/v2/specs/ore-pipeline-batch-v0.1.md`, `docs/ui/v2/plans/32_ore-pipeline-batch-ui.md`, and focused regression coverage.
- Implemented runtime augmentation in the v2 ore pipeline UI: added `src/ore_classifier/augmentation.py`, `[ ] Augmentation [Edit]` controls with grouped color/tone, acquisition-noise, and grinding/polishing artifact settings (scratches, polishing haze, pits/dust), localStorage persistence, API payload support for `/api/uploads/{upload_id}/preprocess` and `/api/runs/start`, `augmentation/augmented.png` upload artifacts, immutable `input/augmented.png` run artifacts, run metadata with exact settings, and an `augmented` viewer layer between `original` and `preprocessed`; focused ore UI tests now cover the augmented-before-preprocessing path.
- Added a server-backed Settings page to the v2 ore pipeline UI: `/settings` is direct-loadable, `GET/PUT /api/settings` persist app-wide defaults in `outputs/ore_pipeline_ui/settings/app_settings.json`, and the page controls language, theme, default preprocessing, default tiling overlay, and repeated session metadata defaults.
- Added localized `(?)` hover/focus hints beside all four v2 ore pipeline preprocessing popup settings: illumination normalization, noise reduction, contrast correction, and panorama image scaling. Updated the preprocessing spec/plan, smoke checklist, and UI regression coverage.
- Hardened the talc browser review app against damaged `current_talc_mask.png` files: sample loading now rebuilds an unreadable or wrong-sized working mask from the reviewed/autodetected mask instead of returning a transient `500`, and the focused talc UI regression test covers this recovery path.
- Refined the v2 ore pipeline metadata Domain tab: Project, Microscope/camera, Objective, scale source, scale value, and scale confidence now appear first under `Session specific`; sample-bound fields are under `Sample specific`; the manual scale input is labeled `Scale value, um/px`; and the training flag now reads as excluding this image from training/validation dataset exports.
- Removed the left-sidebar metadata helper sentence from the ore pipeline UI so the input panel only shows the metadata action button after image selection.
- Added v2-native metadata editor documentation: `docs/ui/v2/specs/ore-pipeline-metadata-editor-v0.1.md` and `docs/ui/v2/plans/30_ore-pipeline-metadata-editor.md` now define and harden the `Edit Metadata...` popup in `apps/ore_pipeline_web.py`, including domain metadata, read-only raw metadata, session defaults, scale guardrails, `POST /api/runs/start` `curated_metadata`, per-run `metadata/curated_metadata.json`, and edit-run inheritance.
- Added `docs/ui/v2/specs/ore-pipeline-preprocessing-control-v0.1.md` and `docs/ui/v2/plans/29_ore-pipeline-preprocessing-control.md`, then replaced the v2 ore pipeline visible preprocessing checklist with compact `[x] Предобработка [Настроить...] [Применить]` controls. The detailed settings now live in a popup, `Apply` checks preprocessing and refreshes the preview, `Start` can skip preprocessing through `preprocessing_enabled=false`, and skipped runs do not expose a preprocessed display layer.
- Recorded the runtime augmentation target explicitly in `docs/ui/v2/plans/28_ore-pipeline-ui.md`: the requested `[ ] Augmentation [Edit]` control is for the v2 ore pipeline UI in `apps/ore_pipeline_web.py`, applies before preprocessing, records exact settings/artifacts in run metadata, and inserts `augmented` between `original` and `preprocessed` in the viewer order.
- Ported the metadata editor into the v2 ore pipeline UI: selected images now expose `Редактировать метаданные...` / `Edit Metadata...`, the modal has Domain/Raw/Session Defaults tabs with conservative scale warnings, saved metadata is sent on `Start`, and each immutable run writes `metadata/curated_metadata.json`; edit-and-recalculate runs inherit the parent curated metadata.
- Updated the ore pipeline left-sidebar History cards: each run now has a small thumbnail column with `Load` directly under it, leaving the rest of the card width for filename, run id, date, and classification text.
- Separated ore pipeline image-loading progress from run progress: large upload and preview-preparation phases now update only the input-panel upload progress row, while the Run progress bar remains reserved for `Start`/active run polling.
- Added thumbnails to the ore pipeline History page; each table row now shows a small run preview, and clicking it opens a modal with the larger stored preview image.
- Fixed the ore pipeline Edit & Recalculate dialog so it refreshes the full run payload before loading editor masks/images, re-registering artifact URLs after server restarts or stale browser state, and shows a localized editor error instead of a blank canvas when prerequisites are missing.
- Moved talc review `Save` and `Save & Next` actions from the right details panel into a top-right action group beside the canvas toolbar.
- Added `Sulfide mask (sulfide/non-sulfide mask segmentation)` to the talc review app's `Base image` selector so reviewers can inspect the sulfide mask as the canvas background while editing talc.
- Added cancelable ore pipeline runs: pressing `Start` swaps the control to a red `Stop` button, `POST /api/runs/{run_id}/cancel` marks active jobs as canceling/canceled, ML subprocess runs are terminated cooperatively, and the UI polling/history flow treats canceled runs as terminal.
- Added a `show tiling` viewer checkbox to the ore pipeline UI. Preprocessing now records a tile manifest for scaled/large images, runs carry it forward, and the canvas can overlay the tile grid on original/preprocessed/result views.
- Enabled all ore pipeline preprocessing options by default and persisted the user's preprocessing preset in local storage, including after page reloads and upload clears.
- Added upload/preview-preparation progress to the ore pipeline UI: large image uploads now show byte-transfer progress, then a localized preparation phase while the server decodes and scales preview pyramids; manual preprocessing preview updates use the same visible progress row.
- Added inline upload warnings to the ore pipeline drop zone: unsupported dropped/selected file extensions are rejected client-side with localized PNG/JPEG/TIFF/RAW guidance, and backend upload failures use the same warning area.
- Added SAM2 point hover previews to the talc browser review app: in `SAM2 point` mode, holding the cursor still for 2 seconds runs SAM2 into an orange preview overlay, and `Apply SAM2` applies that preview or runs/applies the current hover point.
- Changed the ore pipeline History page to a table with filename, date, ore classification, sulfide/non-sulfide and class fractions, plus `Load` and `Remove` actions; `Remove` deletes completed/failed run artifacts through `DELETE /api/runs/{run_id}` while leaving uploads intact.
- Added Delete-key shape removal to the talc browser review app: selected completed polygons or rectangles can be removed with Delete/Backspace, while text fields keep normal delete behavior.
- Added slug URLs to the ore pipeline UI: `/` redirects to `/workspace`, `/workspace` and `/history` are direct-loadable pages, tab clicks update the URL, and browser back/forward restores the matching page.
- Completed English-mode coverage in `apps/ore_pipeline_web.py`: preprocessing labels, class labels, result headings, history/result summaries, and metric row labels now switch to English from the existing immutable run metadata.
- Added Russian/English localization to `apps/ore_pipeline_web.py`: the UI now defaults to Russian, exposes a persistent language selector, and localizes static controls plus main upload/progress/history/editor messages.
- Moved talc browser review controls into one top toolbar ordered `Brush`, `Rectangle`, `Polygon`, `SAM2`, `Undo`, `Zoom In`, `Zoom Out`, `Fit`, with tool-specific parameters on the right and mouse-wheel zoom over the canvas.
- Changed ore pipeline history rows from `Open` to `Load`; loading a run now restores its original upload and preprocessing preset so parameters can be tuned and `Start` can create a fresh immutable rerun.
- Moved Edit & Recalculate tool controls into a top modal toolbar, including Brush/Pan, undo/redo, zoom, fit view, and brush size, leaving the right panel for layer, class, comment, and statistics.
- Styled the ore pipeline `Fix me` action with the theme danger/red background in both light and dark modes.
- Updated `apps/ore_pipeline_web.py` viewer controls: `original`, `preprocessed`, `sulfide`, and `final` are primary layer buttons, unavailable layers are disabled/greyed until their artifacts exist, and side-by-side comparison is now a separate `none/preprocessed/sulfide/final` selector with a draggable splitter and the selected comparison layer on the right.
- Added a dashed SAM2 prompt preview to the talc browser canvas: SAM2 hover now shows the proposed box/point area before clicking, and active SAM2 box drags use the same dashed orange preview styling.
- Updated `apps/ore_pipeline_web.py` input upload card: after selecting an image the drop zone shows a thumbnail with filename/dimensions and an `×` clear button; clearing resets upload/run/results/progress/view controls to the initial state while preserving run history.
- Updated `apps/ore_pipeline_web.py` Edit & Recalculate: layer selection is now tabs, Brush supports left-draw/right-erase, undo/redo is available, editor zoom in/out, fit view, and pan controls were added, and the right panel now shows live pixel/% statistics for sulfide/non-sulfide and final segmentation classes.
- Guarded talc browser SAM2 assist against whole-image fills: `SAM2 box` is now the default prompt, box/polygon prompts are clipped to their prompt bounds, SAM masks covering more than half the image are rejected with a clear status message, browser merge now requires high-value mask pixels instead of any nonzero RGB, and local `unittest` now covers `61` tests.
- Updated talc polygon right-click behavior: right-clicking a draft or completed polygon point now removes that point, while right-clicking elsewhere still cancels the current polygon draft; completed polygon point removal autosaves as `polygon_point_remove`.
- Added a brush hover-radius cue to the talc browser review canvas: Brush mode now shows the current draw/erase footprint as a circle over the image, including brush-size changes and pointer leave cleanup.
- Added dark-mode support to `apps/ore_pipeline_web.py`: a persistent `Theme` selector now supports `System`, `Light`, and `Dark`, with theme-aware panels, controls, toolbar, modal, history rows, and viewer backgrounds.
- Added `apps/ore_pipeline_web.py`, a local browser/canvas UI for the requested image-upload -> preprocessing -> immutable run -> sulfide/final visualization -> CSV/PDF export -> edit-and-recalculate -> history workflow; added `docs/ui/v2/plans/28_ore-pipeline-ui.md` and focused tests, bringing local `unittest` coverage to `59` tests.
- Updated `apps/talc_review_web.py` polygon/rectangle UX: polygons are finalized by clicking the first point, right-click cancels active drafts, rectangles support both drag and two-corner drawing, completed polygon/rectangle regions stay editable with live filled-mask updates until the image changes or `Save` flattens them, and browser smoke plus local `unittest` now cover `59` tests.
- Removed the separate Eraser toolbar tool from the talc browser review UI; Brush is now the single stroke tool, with left mouse drawing talc and right mouse erasing, and local `unittest` now covers `55` tests.
- Added `scripts/evaluate_ore_feature_classifier.py` and tests: a 5-fold image-level ExtraTrees classifier over B2 batch fractions/component aggregates reached macro F1 `0.7439`, weighted F1 `0.7439`, accuracy `0.7420`, and macro AUC OVR `0.8802` on the 345-image deconflicted balanced split; local `unittest` now covers `55` tests.
- Updated the talc browser review Brush interaction: left mouse draws talc, right mouse erases while Brush remains selected, the browser context menu is suppressed during Brush strokes, and local `unittest` now covers `53` tests.
- Optimized component analysis by computing ordinary/fine morphology on padded component bounding boxes instead of full-frame masks per component; this targets the zelda fine-shard CPU bottleneck observed during the deconflicted B2 batch, and local `unittest` now covers `53` tests.
- Added `scripts/merge_official_batch_shards.py` and tests so the live class-sharded zelda B2 run can be combined into one `summary.csv/json` plus `failures.json` before image-level F1/AUC evaluation and threshold calibration.
- Added calibrated-rule config plumbing end-to-end: `--rule-config-json` is now accepted by `scripts/analyze_ore_from_masks.py`, `scripts/run_ore_pipeline.py`, and `scripts/run_official_batch.py`; calibration artifacts from `scripts/calibrate_ore_rules.py` can be applied directly, explicit CLI thresholds override the file, batch summaries record the applied rule thresholds, and local `unittest` now covers `52` tests.
- Added official label audit/deconflicted split tooling: `scripts/audit_official_labels.py` found `56` duplicate-content groups and `24` conflicting-label groups in the labelled official folders; `scripts/build_official_balanced_eval_split.py --label-audit-json --exclude-conflicts --dedupe-sha256` now builds a cleaner `345`-image split (`115` per class); local `unittest` now covers `45` tests.
- Added sulfide-overlap protection to the talc browser review app: `Protect sulfides while drawing` is enabled by default, brush/polygon/rectangle/SAM2 additions are clipped against the loaded sulfide mask without silently changing older overlap, `Subtract sulfides from mask` manually removes any current overlap and autosaves, and metrics now show `Current on sulfide px`. Browser smoke passed on an isolated one-sample workspace, and local `unittest` now covers `45` tests.
- Added analyzed-area denominator support for the ML pipeline: `src/ore_classifier/analyzed_area.py`, `analyzed_mask.png` artifacts, analyzed-vs-full-image fractions, and additive `ore_summary.json` decision margins/warnings. `sulfide_fraction` and `talc_fraction` now use analyzed non-excluded pixels; full-image fractions remain available as `*_fraction_image`; local `unittest` now covers `45` tests.
- Updated polygon draft editing in `apps/talc_review_web.py`: clicking an edge inserts a vertex without Shift, right-clicking a point deletes it without opening the browser context menu, existing points remain draggable, and the status line now confirms add/insert/remove actions. Browser verification passed on `http://127.0.0.1:62534/`, and local `unittest` now covers `45` tests.
- Added `scripts/calibrate_ore_rules.py` and `tests/test_ore_rule_calibration.py`: completed official batch runs can now grid-search deterministic ordinary/fine/talc thresholds from `summary.csv` plus per-run `component_features.csv`, writing explicit calibration JSON/Markdown without rerunning B2 inference; local `unittest` now covers `45` tests.
- Added `docs/notes/2026-07-03-pipeline-improvement-proposals.md`: a multi-agent, current-state-verified pipeline gap review with 19 prioritized proposals in 5 tiers (correctness fixes gating all metrics, the unmeasured image-level F1/talc-±3pp loop, cheap accuracy levers incl. the confirmed random-init B0/B1/B2 finding, 5-minute panorama compliance, stretch data/training upgrades) plus a suggested 36-hour execution order; research only, no code changes.
- Added dark-mode support to `apps/talc_review_web.py`: a persistent `Theme` selector supports `System`, `Light`, and `Dark`; the UI uses theme-aware CSS variables while preserving canvas/mask colors. Browser verification passed for dark/light switching on `http://127.0.0.1:62534/`, and local `unittest` now covers `42` tests.
- Added `EXTERNAL_DATASETS.md` with teammate-facing download/provenance instructions for the official task package and LumenStone S1/S2 retraining data.
- Added the talc-only browser/canvas review app `apps/talc_review_web.py`: it starts from the raw `Области оталькования` folder or a prepared conversion workspace, pairs annotated files with same-filename clean originals, auto-creates `current_talc_mask.png` on first open, edits the talc mask directly with brush/eraser/polygon/rectangle/SAM2 prompt tools, supports undo/autosave, and writes reviewed masks, overlays, patches, and summaries.
- Added `tests/test_talc_review_web.py` for annotated/original pairing, first-open current-mask creation, reviewed save artifacts, reset behavior, theme control, and HTTP endpoints; local `unittest` now covers `42` tests.
- Updated `COMMANDS.md`, `SMOKE_TESTS.md`, and `docs/ui/v2/specs/talc-mask-review-web-app-v0.1.md` to make the new browser/canvas talc review app the preferred UI path and keep the Streamlit talc UI as legacy.
- Added the private GitHub repository location for the clean v2 workspace: `s1mb1o/2026-ore-thin-section-classifier`.
- Added the automatic full-path ore batch implementation: `src/ore_classifier/talc_candidate.py`, `scripts/run_ore_pipeline.py --auto-talc-candidate/--talc-mask`, `scripts/run_official_batch.py`, and `scripts/evaluate_ore_classification.py`.
- Added regression tests for the talc candidate mask and image-level ore classification evaluator; local `unittest` now covers `36` tests.
- Smoke-tested the auto-talc one-image pipeline and one-image official batch path with the commit smoke ResUNet checkpoint under `outputs/commit_smoke_ore_pipeline_auto_talc/` and `outputs/commit_smoke_official_batch/`.
- Added `docs/ui/v2/specs/talc-mask-review-web-app-v0.1.md`, a draft review spec for replacing the unreliable Streamlit mask-editing flow with a talc-only local `http.server` + generated HTML/CSS + vanilla JS/canvas application that pairs MS Paint annotated talc images with same-filename originals, creates/reuses an automatic conversion workspace, supports brush/eraser/polygon/rectangle/SAM2 talc-mask editing, undo, `Save`, and `Save and next`.
- Added original-image path display to the sulfide QA UI: each run now shows the copied run source image, the source `dataset/...` path when it can be inferred from `review_manifest.csv`, and stores the same path metadata in saved review JSON.
- Fixed a talc Streamlit crash where `st.segmented_control` could return `None` for the canvas tool after session-state migration; segmented controls now drop invalid saved values and fall back to their default option.
- Fixed the talc Streamlit canvas compatibility shim for Streamlit 1.50: `streamlit-drawable-canvas` now gets the legacy `streamlit.elements.image.image_to_url` wrapper even when Streamlit exposes `LayoutConfig` instead of `create_layout_config`.
- Documented the canonical v2 iteration workdir in `AGENTS.md` and `docs/session-sync.md`: `/Volumes/T7_2TB/Projects-T7_2TB/2026_Nornikel_Hackaton_v2`.
- Added `requirements-ui.txt` for Streamlit-only review tools and documented that the full `requirements.txt` needs Python >=3.10; this fixes the Python 3.9 `.venv` install failure on `transformers>=5.12.1`.
- Updated `COMMANDS.md` with a `streamlit: command not found` fix: use the existing temporary venv or create/activate a repo-local `.venv`, then launch Streamlit via `python -m streamlit`.
- Added root `COMMANDS.md` with short launch snippets for talc conversion/review, the B2 ore pipeline, sulfide QA, manual review pack generation, the heuristic baseline, and tests.
- Fixed the talc review Streamlit compatibility patch for newer Streamlit internals where `create_layout_config` is replaced by `LayoutConfig`.
- Added `scripts/prepare_manual_review_pack.py` and generated a B2 manual review pack under `outputs/manual_review/b2_balanced_review_pack/`: `9` balanced official-class samples, review panels, source previews, sulfide overlays, probability heatmaps, ordinary/fine overlays, `8` uncertainty crop candidates, `review_manifest.csv/json`, `review_candidates.csv`, and `feedback_template.csv`. The Streamlit sulfide QA app now displays optional review panels, source previews, and heatmaps when present.
- Added the runnable ore pipeline: official-metric evaluator (`IoU`, `F1`, `AUC`, Hausdorff/HD95), tiled binary sulfide inference, component-level ordinary/fine analysis, one-command `run_ore_pipeline.py`, balanced official eval split JSON/CSV, and model/data/run cards under `docs/cards/`. SegFormer-B2 on zelda completed as the current default checkpoint with val sulfide IoU `0.974381`, F1 `0.987024`, AUC `0.998811`, and HD95 mean `23.57 px`; the final B2 demo output is saved under `outputs/inference_demo/b2_final_row_2539589_1/`.
- Added SegFormer-B2 support and completed B2 on zelda as the current best binary sulfide checkpoint: val sulfide IoU `0.974381`, F1 `0.987024`, AUC `0.998811`, HD95 mean `23.57 px`.
- Added a six-image final-B1 visual validation pack note at `docs/notes/2026-07-03-b1-visual-validation-pack.md`; the masks/overlays render, but the current deterministic ordinary/fine thresholds need calibration before image-level class claims.
- Added reusable demo libraries for the researched ideas: `source_fusion`, `review_queue`, `curation`, `component_reports`, `report_cards`, and `scribble_classifier`, with synthetic unit tests and a focused note at `docs/notes/2026-07-03-reusable-demo-libraries.md`.
- Expanded the research mindstorm with additional tooling/workflow ideas from microscopy annotation, dataset curation, material microstructure segmentation, and automated mineralogy sources: scribble-trained shallow pixel classifiers, corrective annotation, FiftyOne/cleanlab-style curation, microscopy-pretrained encoders, μSAM/MatSAM prompt planning, object-level component classification, mineral liberation-style reports, model/data cards, expert-question generation, and annotation-budget simulation.
- Added Reddit/practitioner signals to the research mindstorm note: SAM-oversegmentation cautions for microscopy, flat-field preprocessing, model-assisted annotation loops, active-learning diversity beyond entropy, high-loss pseudo-label cleanup, boundary/ignore-band training ideas, local annotation UX, tiled-inference provenance, and mineral-property explainability cards.
- Added a separate `heuristic_segmentation/` subproject with a non-neural CLI baseline, component ordinary/fine rules, talc candidate mask, overlay/CSV/JSON artifacts, synthetic unit tests, official-image smoke output, and a focused note at `docs/notes/2026-07-03-heuristic-segmentation-subproject.md`.
- Added `docs/plans/27_talc-silicate-support-labeling.md` and implemented optional silicate-support handling in the talc converter: `--silicate-mask-dir`, supported/unsupported talc masks, conservative `talc_positive_core_mask.png`, `silicate_hard_negative_mask.png`, updated summary counts, and synthetic regression coverage.
- Expanded the research mindstorm note after deeper paper search: added OIA protocol framing, enhanced thresholding hard negatives, Snorkel/STAPLE-like weak label fusion, co-teaching for noisy pseudo masks, MIL/CLAM-like ordinary/fine classification, domain-adaptation cautions, large-image tiling references, rare-class losses, and talc dataset caveats.
- Added `docs/notes/2026-07-03-official-metrics-and-panorama-split.md` and updated benchmark/planning docs with organizer guidance: segmentation should track IoU plus Hausdorff/HD95, classification should track F1 plus AUC, and unannotated/unclassified panoramas should not be treated as a labelled ground-truth test set.
- Added `docs/notes/2026-07-03-research-mindstorm-improvements.ru.md` with research-backed Russian mindstorm ideas: disagreement maps, active-learning review queue, SAM2-assisted QA, component passports, decision margins, robustness certificates, weak-supervision training upgrades, and paper/source references.
- Added `docs/benchmarks/01_binary_sulfide_model_benchmark.md`: SegFormer-B2 on zelda is the current best binary sulfide checkpoint with validation sulfide IoU `0.974381`; gx10 ResUNet completed with best observed IoU `0.956436`. Mirrored the B0/B1/B2 and ResUNet checkpoint artifacts locally under `models/binary_sulfide/`.
- Added the talc hard-negative presentation point: absent LumenStone/Petroscope silicate/matrix support on a talc candidate should demote it to `not_talc` or `uncertain`, not positive talc.
- Added binary sulfide training utilities: official manifest generation, LumenStone/official pseudo-label tile dataset builder, ResUNet/SegFormer training script, dataset loader, model code, and smoke-tested both ResUNet and SegFormer-B0 locally.
- Built `outputs/binary_sulfide_dataset_v0` with `8536` tiles (`6948` train / `1588` val) from LumenStone masks and official heuristic pseudo masks.
- Synced v2 code and the binary sulfide tile dataset to gx10 and launched ResUNet training in `tmux` session `nornickel_v2_resunet`, output `outputs/train_resunet_gx10_20260703_004425`; first completed epochs reached sulfide IoU `0.912475` and `0.924633`.
- Prepared the new zelda host `root@161.104.48.181` with synced v2 code/dataset and a project venv. A later retry found the NVIDIA GPU attached, passed a SegFormer-B0 CUDA sanity run, and launched SegFormer-B0 training in `tmux` session `nornickel_v2_segformer_b0`, output `outputs/train_segformer_b0_zelda_20260702_220225`; observed epoch 9 val sulfide IoU `0.948772`.
- Added binary sulfide utility unit tests for LumenStone class-id mapping, brightness pseudo masks, class-id parsing, and tile edge padding; local `unittest` now covers `7` tests.
- Added `docs/notes/2026-07-03-gpu-training-status.md` with the current gx10/zelda training status and blocker.
- Updated `apps/talc_review_streamlit.py` for mask-first talc QA: `Review canvas` now defaults to current mask view, `Brush`/`Erase` are the only stroke-width tools, `Filled polygon` and `Filled box` are editable filled-area tools with polygon vertex drag/add/delete and box corner/edge drag, and coordinate fallbacks are kept under `Advanced`.
- UX-tested the talc Streamlit review flow in browser and fixed interaction issues: workspace selection now uses a stateful segmented control instead of rerun-resetting tabs, edit actions rerun with flash messages so overlays/metrics update immediately, edit workspaces show local current-mask counters, and `Reload base masks` clears unsaved edit history and canvas/shape drafts.
- Added SAM2 as `SAM2 assist` in `Review canvas`: users can place a point or box prompt directly on the canvas background, run `Load/check SAM2`, and apply `Run SAM2` to the current mask.
- Added `presentation.md` with a slide-by-slide explanation of the official OM-only ore-classifier approach.
- Ported the talc blue-line conversion/review block into the v2 layout: `src/ore_classifier/talc_blue_line_converter.py`, `src/ore_classifier/sam2_region_assist.py`, `scripts/convert_talc_blue_lines.py`, `apps/talc_review_streamlit.py`, tests, and `requirements.txt`. Regenerated all `42` `Области оталькования` samples under `outputs/talc_blue_line_conversion`; status counts are `31` `candidate_ok`, `9` `needs_manual_review`, and `2` `sulfide_overlap_review_required`.
- Created the clean v2 workspace for the official OM-only ore-classifier path.
- Added a relative `dataset` symlink to the verified official dataset location.
- Copied the selected official task docs, focused plans, specs, and research notes needed for the current sulfide/talc pipeline.
- Added v2-specific `AGENTS.md`, `README.md`, `docs/session-sync.md`, `ResearchLog.md`, `SMOKE_TESTS.md`, and initial placeholders for `apps/`, `scripts/`, `src/ore_classifier/`, `outputs/`, and `models/`.
